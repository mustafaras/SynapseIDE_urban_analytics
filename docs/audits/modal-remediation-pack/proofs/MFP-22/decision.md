# MFP-22 — owner decision (2026-06-20)

`requiresHumanDecision: true` (name + semver). Both confirmed with the owner via
AskUserQuestion (carried from the MFP-21/MFP-22 branding round — same session):

- **Canonical name** (must match MFP-21): "Synapse IDE — GIS & Urban Analytics Workbench".
- **package.json name** (kebab, aligned with BRAND): `synapse-ide-urban-analytics-workbench`.
- **Release version (semver):** **0.9.0** (owner chose 0.9.0 over the spec's 1.1.0 default —
  pre-1.0 public release). `BRAND.version` already = 0.9.0 (set in MFP-21) — now mirrored in
  package.json.

No tagline invented; description reuses the existing package.json `description`
("Browser-based spatial intelligence platform for urban scientists, planners, and GIS
analysts").
