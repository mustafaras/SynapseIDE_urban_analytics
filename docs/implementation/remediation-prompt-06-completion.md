### Remediation Prompt 06 - Completion Report
- Scope Completed: Refactored GeoAI Lab NL-to-SQL execution away from hardwired demo collections onto explicit live-versus-demo runtime modes, added live queryable-layer inspection from Map Explorer overlays, executed accepted SQL through SpatialDB, preserved deterministic interpretation metadata and safety rejection, and persisted/published accepted results into map and completed-run review surfaces.
- Key Files Added or Updated: `src/centerpanel/Tools/components/GeoAILab.tsx`; `src/engine/geoai/nlp/QueryToSQL.ts`; `src/engine/spatial-db/SpatialDB.ts`; `src/engine/spatial-db/hooks/useSpatialDB.ts`; `src/services/map/MapEngineAdapter.ts`; `src/App.tsx`; `src/centerpanel/Tools/components/__tests__/GeoAILab.test.tsx`; `e2e/geoai-real-data.spec.ts`; `docs/implementation/prompt-14-completion.md`; `docs/implementation/prompt-status-ledger.md`; `docs/implementation/module-matrix.md`; `docs/release/visual-completeness-checklist.md`; `docs/release/release-candidate-validation.md`; `docs/release/known-risks-and-limitations.md`.
- User-Facing Surfaces Added or Corrected: GeoAI Lab now shows explicit `Live project data` versus `Demo collections` query modes, lists available live layers with alias binding and schema summaries, explains readiness or missing-table failures directly in the UI, and publishes accepted live-query results into Map Explorer and completed-run review.
- Runtime Truthfulness Improvements: The UI no longer implies live querying while secretly executing against demo fixtures, unavailable tables are rejected with explicit messaging instead of silent fallback, mutating/unsafe prompts remain blocked, and saved outputs now retain the query prompt, interpretation, SQL, referenced tables, runtime mode, and result summary.
- Validation Performed: PASS `npm run typecheck`; PASS `npm run build`; PASS `npm run test`; PASS targeted Vitest coverage for `src/engine/geoai/nlp/__tests__/QueryToSQL.test.ts` and `src/centerpanel/Tools/components/__tests__/GeoAILab.test.tsx`; PASS `npm run test:e2e -- e2e/geoai-real-data.spec.ts`.
- Residual Risks: Live execution currently depends on project data already being available as queryable GeoJSON overlays in Map Explorer rather than every possible dataset surface; deterministic entity extraction still relies on a fixed alias vocabulary; full cross-surface schema federation remains follow-up work.
- Follow-Up Required Before Next Prompt: Prompt 07 should reuse the same explicit real-versus-demo runtime language and persisted provenance model rather than introducing a separate fallback contract for object detection.