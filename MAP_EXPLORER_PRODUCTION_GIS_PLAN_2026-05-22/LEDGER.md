# LEDGER — Map Explorer Production GIS prompt ladder

Single source of execution state + the resume point for any chat (especially a fresh one with no memory). Date started: 2026-05-22.

> **This ledger does not auto-run anything.** It is *discovered* via the `CLAUDE.md` "Active operating pack" pointer (auto-loaded every session) and via `README.md`. Once an agent opens this pack, this is the first file to read: it tells the agent how to boot safely and which prompt is next. Treat it as the "where are we / what next" record.

---

## ▶ Resume in a new chat (do this first)

0. **Canonical trunk = `gis/map-explorer-production-prompts`.** Run
   `git checkout gis/map-explorer-production-prompts` first. This branch is kept
   fast-forwarded to the latest completed prompt (see Update protocol step 6), so
   the working-tree ledger you read here is authoritative. Per-prompt checkpoint
   branches (`gis/pNN-*`) are where each slice was built, but you do **not** resume
   from them — they feed the trunk, not the other way around. The WelcomeModal
   orbital redesign commit (`aa0be7d`) must remain in ancestry; do not resume from
   the older `4d47df5` base.
1. **Reconcile ledger with git before trusting it (anti-amnesia guard).** Run
   `git log --oneline -5` and compare the latest `feat(map…)` commit to the highest
   `[x]` in the Status table below. If they disagree, the checked-out branch is
   STALE — run `git branch -v`, find the highest `gis/pNN-*` tip, and
   `git merge --ff-only <that branch>` onto the trunk before continuing. (This is
   exactly the drift that stalled the trunk at Prompt 2 on 2026-05-26; see Drift notes.)
2. Open [15_AGENT_EXECUTION_PROMPTS.md](15_AGENT_EXECUTION_PROMPTS.md) → read the **"⚠️ Cold-start protocol (anti-amnesia)"** section and copy its **BOOT BLOCK**.
3. Read the **Status** table below; pick the lowest-numbered prompt that is `TODO` (respecting the dependency graph in the prompt file's "Sequencing Cheat Sheet").
4. Paste the BOOT BLOCK, then paste that prompt, into the new chat.
5. The agent must: read the named docs/contracts/fixtures, create a checkpoint branch `gis/p<NN>-<slug>`, deliver the prompt's **Proof**, run its **Validate** commands, then **append a row to the Done Log below and flip the Status checkbox** in the same commit. After it lands, **fast-forward the trunk onto it** (Update protocol step 6).

If you only have this ledger in front of you and nothing else: the standing rules are summarised under "Non-negotiables (mirror)" near the bottom — but still open the prompt file's Agent Contract v2 for the full list.

---

## Status (0 → 64)

Legend: `[x]` done · `[~]` in progress · `[ ]` TODO · `[!]` blocked (see Done Log).

**Track A — Foundation**
- [x] 0 — Bootstrap: contracts + fixtures + conventions ✅ verified
- [x] 1 — Baseline inventory + canonical-surface ADR ✅ verified
- [x] 2 — Decompose `MapExplorerModal.tsx` ✅ verified
- [x] 3 — Store slices + selectors ✅ verified
- [x] 4 — `MapSourceRegistry` V1 ✅ verified
- [x] 5 — Import preflight + profiling + support matrix ✅ verified (profiling/preflight green; e2e 3/3 green after fixing a real QA re-eval freeze on import — see Done Log + Drift notes)
- [x] 6 — `MapProjectionService` + `ExecutionCrsPlanner`
- [x] 7 — `CrsPreflight` gate ✅ verified
- [x] 8 — CRS correction UI + projection suggestion ✅ verified
- [x] 9 — Map command lifecycle (`MapActionExecutor`) ✅ verified
- [x] 10 — Layer inspector workbench ✅ verified
- [x] 11 — Attribute table + selection sync ✅ verified (map/type/lint/e2e green; `test:analytics` runner hang noted under Drift notes)
- [x] 12 — Style editor + legend contract ✅ verified
- [x] 13 — Workerized geometry operations ✅ verified
- [x] 14 — AOI + vertex editing ✅ verified
- [x] 15 — Selection tools + query planner ✅ verified
- [x] 16 — `MapUrbanBridgeService` V1 ✅ verified
- [x] 17 — Urban method compatibility rail ✅ verified
- [x] 18 — Map context → Urban recommendations ✅ verified
- [x] 19 — Evidence publication hardening ✅ verified
- [x] 20 — Review timeline + action export ✅ verified
- [x] 21 — External service production path ✅ verified

**Track B — Publication / Perf / Release**
- [x] 22 — Publication figure composer V1 ✅ verified
- [x] 23 — Performance budgets + render diagnostics ✅ verified
- [x] 24a — Processing toolbox: registry + UI + 3 reference tools ✅ verified
- [x] 24b — Processing toolbox: wrap services (≥12) ✅ verified
- [x] 24c — Processing toolbox: premium styling + blocked states ✅ verified
- [x] 25a — Model builder: graph + deterministic run ✅ verified
- [x] 25b — Model builder: batch + IDE artifact + Urban publish ✅ verified

**Track C — Operator surfaces**
- [x] 26 — Catalog + connection manager ✅ verified
- [x] 27 — Professional contents tree ✅ verified
- [x] 28 — Field statistics + safe field calculator ✅ verified

**Track D — Layouts + 3D**
- [x] 29 — Layout composer V2 + map book ✅ verified
- [x] 30 — 3D scene runtime + 2.5D extrusion ✅ verified
- [x] 31 — Block/parcel model + zoning rule engine ✅ verified
- [x] 32a — Zoning envelope (deterministic, CRS-gated) ✅ verified
- [x] 32b — Massing alternatives + scenario manager ✅ verified
- [x] 33 — Sunlight/shadow + 3D scenario evidence ✅ verified
- [x] 34 — 3D block + scenario interaction design ✅ verified

**Track E — Premium design + motion + visual QA**
- [x] 35 — Extend GIS token layer (status/density/motion) ✅ verified
- [x] 36 — Workbench shell hardening + shared primitives ✅ verified
- [x] 37 — Catalog/contents/inspector visual pass ✅ verified
- [x] 38 — Table/toolbox/layout visual pass ✅ verified
- [x] 39 — Motion system + reduced-motion gate ✅ verified
- [x] 40 — Visual QA + design release gate ✅ verified

**Track G — Capability depth**
- [ ] 43 — GeoPackage/Shapefile/KML/GPX import
- [ ] 44 — Large vector streaming + extent query
- [ ] 45 — Raster/GeoTIFF render + histogram + QA
- [ ] 46 — Temporal layers: playback + frame export
- [ ] 47 — Labeling engine
- [ ] 48 — Advanced cartography (bivariate/dot-density)
- [ ] 49 — Topology validation + guided repair
- [ ] 50 — Join/relate preview
- [ ] 51 — Reprojection cache
- [ ] 52 — Multi-scale vector tile pipeline

**Track H — Enterprise / modern GIS depth**
- [ ] 53 — Command palette + keyboard system
- [ ] 54 — Undo/redo stack
- [ ] 55 — Plugin/extension registry
- [ ] 56 — Telemetry/observability + error recovery
- [ ] 57 — Offline reproducible package export
- [ ] 58 — AI guardrails for NL query + copilot
- [ ] 59 — Collaboration: shared review sessions
- [ ] 60 — Terrain/elevation + CityJSON/3D Tiles
- [ ] 61 — View corridors + section/cut planes
- [ ] 62 — Raster/temporal/3D evidence visual states

**Track F — Close-out**
- [ ] 63 — Documentation + support matrices
- [ ] 64 — Release candidate gate

---

## Done Log (newest first)

| Date | Prompt | Branch | Commit(s) | Proof |
| --- | --- | --- | --- | --- |
| 2026-05-28 | 40 — Visual QA + design release gate | `gis/p40-visual-qa` | this checkpoint | Playwright `e2e/map-visual-qa-p40.spec.ts` @smoke (8 test groups): (1) desktop shell — canvas region + activity rail visible; (2) bidirectional blank-canvas detection proof — white overlay injected → pixel diversity ≤10 unique bytes (detected as blank), real canvas → >5 unique bytes (passes guard); (3) floating panel screenshots for catalog/toolbox/layout designer/inspector — each carries panelIn class; (4) reduced-motion visual state — matchMedia=true + animationDuration "0s" on motion-bearing panels; (5) 768×1024 tablet overlap check — activity rail right edge ≤ canvas left edge; (6) 1280×600 short-viewport clipped-text — dialog header within viewport; (7) hidden-caveat regression — demo layers always expose visible demo/synthetic chip; (8) layer-row motion class + stable hover dimensions. Vitest `mapVisualQA.test.ts` (35 tests, @vitest-environment node): pixel-diversity detector (6 tests), isCanvasBlank bidirectional proof (6 tests), rectsOverlap overlap guard (4 tests), isWithinViewport clipped-text guard (4 tests), motion.module.css class contract (14 tests + 1 summary = 15). `docs/map-visual-qa-checklist.md` — 6-section manual review checklist (desktop/tablet/short-viewport/high-contrast/reduced-motion/CI gates) with reviewer sign-off table. `npm run typecheck` clean. Screenshots verified in `e2e/__screens__/` (shell, toolbox panelIn, inspector+emptyState fadeIn, attribute table featurePulse, activity rail accentGrow). |
| 2026-05-28 | 39 — Motion system + reduced-motion gate | `gis/p39-motion` | this checkpoint | Applied 7 motion classes from `motion.module.css` to UI components: `GisEmptyState` → `.fadeIn` on root div; `GisStatusChip` → `.statusFlash` on span (callers change `key` to re-trigger); `GisProgressBar` → `.progressFill` on fill div (determinate + non-reduced only); `GisIconButton` → replaced `boxShadow: inset 2px 0 0` accent with an absolutely-positioned `<span className={accentGrow}>` that mounts/unmounts with `active` state (position:relative added to button, no layout dimension change); `MapCatalogPanel`, `LayerInspector`, `MapLayoutDesignerPanel`, `MapProcessingToolboxPanel` → `.panelIn` on root element (all return null when !visible so animation fires on mount); `MapLayerManager` `LayerRow` → `.layerFade` on row div; `MapAttributeTable` selected row → `.featurePulse` className + `outline: 1px solid` (outline does not affect layout dimensions). Vitest `mapMotionSystem.test.ts` (19 tests, `@vitest-environment node`): collects animated class names from raw CSS, asserts each has a `@media (prefers-reduced-motion: reduce)` counterpart, verifies `animation: none` + `transition: none` in block, verifies all keyframes are referenced. Playwright `e2e/map-motion-p39.spec.ts` @smoke (2 tests): emulates `prefers-reduced-motion: reduce`, verifies `matchMedia` returns true, checks `getComputedStyle(panelElement).animationDuration === "0s"`, verifies no offsetWidth/offsetHeight change on layer-row hover. `npm run typecheck` clean. 51 vitest map tests green (mapWorkSurfaceVisualPass 11, mapOperatorVisualPass 16, MapAttributeTable 5, mapMotionSystem 19). |
| 2026-05-28 | 38 — Table/toolbox/layout visual pass | `gis/p38-worksurface-visual` | this checkpoint | `MapAttributeTable`: close button → `GisIconButton label="Close attribute table"`; empty-attributes div → `GisEmptyState title="No attributes" compact`; null/undefined cell values now render as italic styled `null` span instead of empty string; profile drawer "Type" stat card replaced with `GisStatusChip` (numeric→ready, temporal→caveat, boolean→demo, else→unknown); removed unused `iconButtonStyle` const. `MapProcessingToolboxPanel`: close button → `GisIconButton label="Close processing toolbox"`; "No tools match" `<p>` → `GisEmptyState title="No tools found"`; "Select a tool" `<p>` → `GisEmptyState title="Select a tool"`; hand-rolled `progressTrackStyle`/`progressFillStyle` divs → `GisProgressBar` with `value={100}` (applied) or `value={8} color={MAP_COLORS.error}` (blocked) + `data-testid="processing-run-progress"`; removed `progressTrackStyle`, `progressFillStyle`, `iconButtonStyle` consts. `MapLayoutDesignerPanel`: close button → `GisIconButton label="Close layout designer"`; "Blocked" section header now includes `GisStatusChip status="blocked"` chip; each blocker row replaces `AlertTriangle` icon with `GisStatusChip status="blocked" label={gap.label}`; "Warnings" section header includes `GisStatusChip status="caveat"`; each warning row has `GisStatusChip status="caveat" label={gap.label}`. 11 unit tests (`mapWorkSurfaceVisualPass.test.tsx`): close button labels, empty states, null cells, progress bar, blocked preflight visible before run, dialog role, page tabs. Playwright `e2e/map-shell-p38.spec.ts` @smoke: toolbox close button + blocked preflight pre-run, attribute table close button. `npm run typecheck` clean. |
| 2026-05-28 | 37 — Catalog/contents/inspector visual pass | `gis/p37-operator-visual` | this checkpoint | `MapCatalogPanel`: close button → `GisIconButton label="Close catalog"`; health spans → `GisStatusChip` (9-way `catalogHealthToGisStatus` mapping: restored/live/cached→ready; recoverable/metadata-only→caveat; unavailable/cors/auth/rate-limit→blocked; external-reference/offline→external-offline; demo→demo; untracked/unknown→unknown); empty category `<p>` → `GisEmptyState compact`; `<strong>` gains `.itemTitle` truncation class + `title` attribute; `.itemSummary` two-line clamp. `MapContentsTreePanel`: close button → `GisIconButton label="Close contents tree"`; scale range span → `GisStatusChip status={inRange?"ready":"caveat"}`; publication readiness span → `GisStatusChip status={pub==="ready"?"ready":pub==="blocked"?"blocked":"caveat"}`; empty state → `GisEmptyState icon={<FolderTree>} title="No layers" compact`. `LayerInspector`: removed hand-rolled `closeButtonStyle`; close button → `GisIconButton label="Close layer inspector"`; custom `tabBarStyle`/`tabButtonStyle` tab bar → `GisTabs` with `tabTestIdPrefix="map-layer-inspector-tab"` (each tab gets `data-testid={tabTestIdPrefix}-{id}` from new `GisTabs.tabTestIdPrefix` prop); `bodyStyle` div preserved inside `GisTabs` children; 4 inline empty-state `<span style={unknownStyle}>` → `GisEmptyState` (no source handle, no schema fields, QA not run, no analysis lineage). `GisTabs` extended with `tabTestIdPrefix?: string` prop. 16 unit tests in `mapOperatorVisualPass.test.tsx` cover all three surfaces: close button aria-label, GisEmptyState role=status, GisTabs data-testid per tab, tablist aria-label, clicking schema tab activates panel, source-tab empty state, initialTab prop, layer name in inspector. Playwright `e2e/map-shell-p37.spec.ts` @smoke: seeds layer, opens inspector, asserts `data-testid="map-layer-inspector-tab-*"` tabs, close button accessible label, screenshots to `p37-layer-inspector.png`. `npm run typecheck` clean. |
| 2026-05-28 | 36 — Workbench shell hardening + shared primitives | `gis/p36-shell-primitives` | this checkpoint | New `src/centerpanel/components/map/ui/` primitives package: `GisIconButton` (accessible icon button — aria-label required, disabled+disabledReason exposes reason via `data-disabled-reason`+`title`), `GisStatusChip` (semantic status chips via MAP_STATUS_TOKENS), `GisTabs` (role=tablist/tab/tabpanel with arrow-key navigation + disabled-reason pattern), `GisSectionHeader` (configurable h2–h4 with actions slot + separator), `GisPropertyGrid` (dl-based key/value grid with highlight states), `GisToolbar`+`GisToolbarDivider` (role=toolbar with pill/inline modes), `GisEmptyState` (role=status live region, icon+title+description+action), `GisProgressBar` (role=progressbar, determinate/indeterminate, reducedMotion prop), `GisTooltip` (CSS-positioned tooltip on focus/hover). Extended `MapWorkspaceShell.tsx`: new `MapActivityRail` (VS Code–style vertical nav strip, uses `GisIconButton` with `mapStyles.sidePanelRowActive` inset accent for active items, supports top+bottom item groups) and `MapCommandBar` (slim top bar with breadcrumb/title + trailing role=toolbar action buttons). Three panels adopt primitives: `Scene3DInteractionStrip` replaces hand-rolled mode buttons with `GisIconButton`; `ScenarioComparisonStrip` replaces the header `<div>` with `GisSectionHeader`; `MapReviewTimelinePanel` replaces the inline empty-state block with `GisEmptyState`. 12 unit tests (`mapShellPrimitives.test.tsx`): GisIconButton aria-label/aria-pressed/disabled/data-disabled-reason; MapActivityRail nav role + item labels + disabledReason; MapCommandBar toolbar + disabled action; GisEmptyState role=status; GisSectionHeader heading level + actions; GisProgressBar aria values (determinate + indeterminate); GisTabs tablist/tabpanel associations + disabled-reason; GisToolbar role. Playwright `e2e/map-shell-p36.spec.ts` @smoke: shell screenshot; disabled command bar action exposes reason via `data-disabled-reason` + `title` on hover; all activity rail buttons have non-empty aria-labels. `npm run typecheck` clean; no bare hex in new ui/ files. |
| 2026-05-28 | 35 — Extend GIS token layer (status/density/motion) | `gis/p35-tokens` | this checkpoint | `MAP_STATUS_TOKENS` added to `mapTokens.ts` with 9 semantic statuses (ready/caveat/unknown/demo/synthetic/external-offline/stale/blocked/running) each carrying `text`/`bg`/`border` aliases resolved entirely through `MAP_COLORS` (no bare hex). `MAP_DENSITY` presets (compact/default) carry `rowHeight`/`cellPadding`/`fontSize`/`gap`/`iconSize`. `GIS_STATUS_KEYS` constant for safe iteration. New `src/centerpanel/components/map/design/` package: `motion.module.css` (7 animation classes with full `@media (prefers-reduced-motion: reduce)` counterparts), `index.ts` re-exporting the existing `usePrefersReducedMotion` hook + new tokens, `GisTokenStatusDemo.tsx` demo component rendering every status chip with motion-class toggling via the hook. Unit tests `mapTokenStatus.test.ts`: 8 tests — all 9 status keys covered, text/bg/border string fields validated, no bare hex literals in any status token, ready≠blocked colors, blocked=external-offline error text, compact rowHeight < default, density field types, `usePrefersReducedMotion` returns `true`/`false` under `matchMedia` mock. Playwright `e2e/map-token-status.spec.ts`: all 9 status chips visible, `data-reduced-motion="true"` set when emulated, `window.matchMedia` returns `true`, chip colors differ by semantic role. `npm run typecheck` clean; `npm run color:guard:changed` exit 0 (5 pre-existing fallback literals in mapTokens.ts, all in allowed `fallback` category). |
| 2026-05-27 | 34 — 3D block + scenario interaction design | `gis/p34-3d-design` | this checkpoint | Extended `useScene3DStore` with `Scene3DInteractionMode` type (8 modes: inspect/select/measure/edit-height/compare/sun-shadow/section/camera-bookmark), `INTERACTION_MODES` constant array, `CameraBookmark` interface, `interactionMode`/`setInteractionMode`/`cameraBookmarks`/`addCameraBookmark`/`removeCameraBookmark` state + actions (persisted). New `Scene3DInteractionStrip.tsx`: compact floating horizontal strip (top-left, `data-position="top-left"` — never occludes center canvas), 8 mode buttons with `aria-pressed`, dividers between groups, camera-bookmark popover with add/remove, `usePrefersReducedMotion` hook (transitions → "none" under `prefers-reduced-motion: reduce`). New `ScenarioComparisonStrip.tsx`: floating draggable panel showing sun-shadow timeline hour chips, massing scenarios side-by-side with GENERATED badge (dashed border, caveat chip — never styled as real), FAR/coverage/height compliance badges, shadow scenario coverage rows, and footer assumption chips (flat terrain / demo runtimeMode / projected CRS required / generated ≠ surveyed). Wired into `MapExplorerModalComposition` via `toggle-3d-interaction-strip` + `toggle-3d-comparison-strip`. 13 vitest unit tests (INTERACTION_MODES: 8 entries, unique, labels; store: interactionMode default/set/cycle, bookmarks CRUD). 6 Playwright @smoke e2e tests: 8 buttons visible, mode toggle aria-pressed, top-left position, strip assumptions footer, timeline chips, generated badge distinct. `npm run typecheck` clean; `npm run lint:errors` clean; 57 files / 549 tests green. |
| 2026-05-27 | 33 — Sunlight/shadow + 3D scenario evidence | `gis/p33-sunshadow` | this checkpoint | New `src/services/map/scene3d/SolarPositionService.ts`: `computeSolarPosition(lat, lon, isoDateTime)` → `{altitudeDeg, azimuthDeg, zenithDeg}` via simplified declination+hour-angle formula. New `src/services/map/scene3d/SunShadowEngine.ts`: `computeShadowAnalysis({buildings, heightsM, parcelAreaM2, solarPosition, ...})` → per-building shadow polygons (footprint shifted along anti-solar vector, length = height / tan(altitude)), coverage ratio, `sunBelowHorizon` flag, full `ShadowAssumptions` (solarModel, verticalDatum, runtimeMode="demo"). New `src/stores/useSunShadowStore.ts` (Zustand persist `urban.config.sunshadow`): timeline hours, active hour/datetime, lat/lon, scenarios, `setActiveHour`, `addScenario`, `publishEvidence` → `ShadowEvidencePayload`; `partialize` strips `shadowPolygon` arrays. New `src/centerpanel/components/map/scene3d/SunShadowPanel.tsx`: hour chips with live altitude labels (`data-testid="sunshadow-hour-chip"`), solar position display, scenario rows with coverage bar, publish button. Wired into `MapExplorerModalComposition` (`toggle-sunshadow-panel`). 11 solar tests + 16 shadow tests (27 total); full map suite 47 files / 397 tests green; `test:analytics` 1130 green. Playwright `map-sunshadow.spec.ts` 4 @smoke tests: solar ordering, 45° shadow length ≈ 10 m, demo runtimeMode, evidence assumptions. `npm run typecheck` clean. |
| 2026-05-27 | 32b — Massing alternatives + scenario manager | `gis/p32b-massing` | this checkpoint | New `src/services/map/zoning/MassingEngine.ts`: deterministic `generateMassingAlternative()` — CRS-gated via `computeZoningEnvelope`, djb2-hash id, N rectangular buildings placed in envelope strips, FAR/coverage/height compliance, caveats. New `src/stores/useMassingStore.ts` (Zustand persist `urban.config.massing`): `addScenario`, `removeScenario`, `setActiveScenario`, `generateComparison()` → full `MapScenarioComparisonEvidenceMetadata`; `partialize` strips `buildingGeometries`. New `src/centerpanel/components/map/zoning/MassingScenarioPanel.tsx`: floating draggable panel — scenario list with compliance badges, add-scenario form (floors/coverage/count/baseline), side-by-side comparison table + evidence button (≥2 scenarios); mapTokens only. 11 unit tests: projected CRS OK, EPSG:4326 blocked, determinism (same params → same id + FAR), coverage/FAR/height compliance. Playwright `map-massing.spec.ts`: store-driven — 2 scenarios added, both visible, generateComparison() produces 2 candidates. `npm run typecheck` clean; 45 map service files / 370 tests green. |
| 2026-05-27 | 32a — Zoning envelope (deterministic, CRS-gated) | `gis/p32a-envelope` | this checkpoint | New `src/services/map/zoning/ZoningEnvelopeEngine.ts`: `insetRing()` (inward-offset of a CCW polygon ring by setback metres using per-edge inward normals + consecutive offset-line intersection, with collapse detection via per-vertex inward-side check against all offset lines), `computeZoningEnvelope()` (CrsPreflight gate → setback inset → buildable area → capacity = maxFAR × buildableAreaM2 → `Feature<Polygon>` envelope geometry), `computeZoningEnvelopeBatch()`. `ZoningEnvelopeEngine` namespace export. 12 unit tests in `ZoningEnvelopeEngine.test.ts`: known parcel + setback yields expected area within epsilon (100×100 parcel, 5 m setback → 8100 m²), deterministic (same inputs → identical output), geographic CRS blocked, null CRS blocked, collapse with large setback (8 m on 10 m parcel), capacity = maxFAR × buildableAreaM2, zero setback = full parcel area, batch mode, envelope geometry properties. Full map service suite 44 files / 359 tests (+ 2 skipped) green. `npm run typecheck` clean. |
| 2026-05-27 | 31 — Block/parcel model + zoning rule engine | `gis/p31-zoning` | this checkpoint | New `src/services/map/zoning/ZoningRuleEngine.ts`: `shoelaceRingArea()` + `polygonAreaM2()` (projected coords only), `computeParcelMetrics()` (CrsPreflight gate — EPSG:4326/null → blocked, EPSG:32635+ → FAR + coverage + capacity), `computeParcelMetricsBatch()`, `createZoningRule()`, `createZoningAssignment()`. Standalone `src/stores/useZoningStore.ts` (Zustand persist `urban.config.zoning`) — rule library, active parcel layer, assignments, metricsIndex. `src/centerpanel/components/map/zoning/ZoningRulesPanel.tsx`: add/remove rules, assign to selected parcel, metrics grid with FAR/coverage/capacity + CRS label + caveats — mapTokens only, no Tailwind. 15 unit tests (`ZoningRuleEngine.test.ts`); full 79-file map suite (703 tests) clean. Playwright `map-zoning.spec.ts` covers rule add, assign + CRS label, geographic-CRS block, FAR/coverage math. `npm run typecheck` clean. |
| 2026-05-27 | 30 — 3D scene runtime + 2.5D extrusion | `gis/p30-3d-runtime` | this checkpoint | New `src/services/map/scene3d/Map3DSceneController.ts`: `analyseExtrusion()` detects height/floor fields, `buildingConfigFromAnalysis()` derives deck.gl `BuildingConfig`, `inspectBuildings()` produces per-feature inspector entries, `buildScene3DMetadata()` records runtime mode + VoxCity-compat QA. Standalone `src/stores/useScene3DStore.ts` (Zustand persist `urban.config.scene3d`) manages mode (2d/2.5d/3d), active layer, selection, inspector entries, height-field overrides, and scene metadata publication. `src/centerpanel/components/map/scene3d/Scene3DPanel.tsx`: draggable floating panel with mode switcher, extrusion config, caveat display, building inspector, and metadata publish action — mapTokens only, no Tailwind. 15 unit tests pass (`Map3DSceneController.test.ts`); full 78-file map test suite (688 tests) clean. Playwright `map-scene3d.spec.ts` covers height detection, missing-height caveat, selection sync, and metadata publication. `npm run typecheck` clean. |
| 2026-05-27 | 29 — Layout composer V2 + map book | `gis/p29-layout-v2` | this checkpoint | V2 additions appended to `MapLayoutComposer.ts` (`MapLayoutPreset`, `LAYOUT_PRESETS`, `MapPageInput`, `MapBookPage`, `MapBookSpec`, `MapFigureRestoreMetadata`, `composeMapBook`, `serializeLayoutRestoreMetadata`, `restorePageInputsFromMetadata`). New `MapLayoutDesignerPanel.tsx` multi-page layout designer (up to 6 pages, page tabs, per-page title/narrative/inset-map/slots, composition toggles, blockers/warnings, preset selector, page-count footer with export button). `MapExplorerModalComposition` now mounts `MapLayoutDesignerPanel` in place of `MapFigureComposerPanel` via the same `showFigureComposer` toggle. `npm run typecheck` clean; `npm run test src/services/map/__tests__/MapLayoutComposerV2.test.ts` 4/4 passed; `npm run test src/centerpanel/components/map` 367/367 passed; `npm run lint:errors` clean; no Tailwind in centerpanel. |
| 2026-05-26 | 28 — Field statistics + safe field calculator | `gis/p28-fieldcalc` | this checkpoint | `src/centerpanel/components/map/table/` now provides numeric/categorical/temporal field profiles plus a sandboxed allowlisted calculator that rejects `eval`, `require`, and constructor/property-access escapes. `MapExplorerModalComposition` turns calculator output into a real derived analysis layer with provenance + scientific QA, retargets the open attribute table to that derived layer, and the Map→Urban bridge now carries per-field null/non-null/total counts so `context/dataFitness.ts` can score missingness when the summary is complete. **Proof (Playwright):** `npx playwright test e2e/map-modal-layout.spec.ts -g "creates a derived attribute layer with a provenance badge from the sandboxed calculator"` passes: compute `value * 2`, assert the derived provenance badge and new `value_x2` field. Validation clean: `npm run typecheck`, `npx vitest run src/centerpanel/components/map`, `npm run test:analytics`. |
| 2026-05-26 | 27 — Professional contents tree | `gis/p27-contents` | this checkpoint | New CSS-Module `contents/` panel exposes grouped layer rows, active-layer accent, visible/selectable/editable and publication badges, inline QA warnings, scale ranges, definition filters, duplicate, source-repair, properties, and reorder actions. A typed `MapLayerContentsState` lives in lightweight `LayerMetadata`, so project snapshots persist groups/order/scale/filter flags without putting geometry in Zustand persistence; map rendering consumes a derived contents projection that scale-hides and feature-filters layers without mutating original source data. Duplication retains source identity, QA and provenance while reusing the same geometry payload. `npm run typecheck`, `npm run lint:no-tailwind-centerpanel`, `npm run lint:errors`, and `npx vitest run src/centerpanel/components/map src/stores` (498 passed) clean. **Proof (Playwright):** `npx playwright test e2e/map-contents.spec.ts` 1/1 passes: group two layers into `Priority Review`, reorder them, save via the project snapshot service, reload the browser, restore state, and assert the group and order persist. |
| 2026-05-26 | 26 — Catalog + connection manager | `gis/p26-catalog` | this checkpoint | New CSS-Module `catalog/` surface groups project, imported, external, worker/DuckDB, generated and explicitly synthetic demo sources; each item exposes restore/service health and actionable repair/reconnect paths. Demo-pack insertion now creates durable `SourceHandle` records before adding its nine layers, from both Catalog and the existing layer-manager entry point, while URL/service connections are registered and health-checked through `MapConnectionRegistry`; the registry now rejects credential-bearing endpoint/tile query URLs, and layers added through prior external-service paths are also brought into the source registry. `npm run typecheck`, `npm run lint:no-tailwind-centerpanel`, `npx vitest run src/centerpanel/components/map` (356 passed), `npx vitest run src/services/map` (312 passed / 2 skipped), and `npm run lint:errors` clean. **Proof (Playwright):** `npx playwright test e2e/map-catalog.spec.ts` 1/1 passes: open Catalog, assert `DEMO / SYNTHETIC`, add the demo pack, then assert `Fatih Demo Blocks` appears with `Demo / restored` source state. |
| 2026-05-26 | 25b — Model builder: batch + IDE artifact + Urban publish | `gis/p25b-model-batch` | this checkpoint (joint 25a/25b commit) | Model templates now batch safely across bound layer/AOI targets with separate result manifests; exporting a successful result builds a reference-only Synapse IDE Python artifact from the model manifest through `MapCodeArtifactRequestService`, then sends a summary-only Map→Urban payload that registers an immutable Urban model-result evidence artifact carrying the manifest id/hash, output/source layer references, step count, batch target count, and conservative `demo`/`unknown` runtime mode. `npm run typecheck`, `npx vitest run src/services/map` (311 passed / 2 skipped), `npm run test:analytics` (1129 passed), `npx vitest run src/centerpanel/components/map` (354 passed), `npm run lint:errors`, and `npm run lint:no-tailwind-centerpanel` clean. **Proof (Playwright):** `npx playwright test e2e/map-model-builder.spec.ts` 1/1 passes: run two layer-bound batch targets, export the active result, assert a bridge-origin IDE `.py` tab containing the model manifest hash, and assert the visible Urban evidence row `Map model result: Transit access coverage`. |
| 2026-05-26 | 25a — Model builder: graph + deterministic run | `gis/p25a-model-core` | this checkpoint (joint 25a/25b commit) | New `src/services/map/model/MapModelBuilder.ts` models source bindings, ordered Prompt-24 processing-tool steps, step-output bindings, saved definitions, deterministic execution/manifest hashes, and transaction-like preflight planning so a blocked downstream step commits no partial map layers. Processing outputs now propagate resolved CRS metadata so a legal projected `buffer → intersect` chain stays CRS-valid. New CSS-Module `modelBuilder/` panel is reachable from the Map toolbar, constructs the ordered graph, saves/reruns, reports identical hash state, and exposes batch/export controls. **Proof (Playwright):** the Prompt 25 spec saves `buffer → intersect`, reruns it, and asserts `Saved rerun identical` plus the unchanged manifest hash. Targeted model/panel tests cover serialization/rerun determinism, blocked no-partial-commit behavior, and the interactive chain. |
| 2026-05-26 | 24c — Processing toolbox: premium styling + blocked/caveat states | `gis/p24c-toolbox-design` | this checkpoint | Premium pass on the toolbox panel (all `mapTokens` inline styles, no Tailwind/hex): a runtime-mode chip (`processing-tool-runtime-chip`) with a friendly label (`main-preview`→"Main-thread preview", `worker`→"Background worker", `geos-wasm`→"GEOS (wasm)", `duckdb`→"DuckDB (wasm)") + CRS-required/No-CRS chip, blocked **and** caveat reasons rendered from the live pre-run preview (`processing-preflight-blocked` / `processing-preflight-caveats`, shown before any run — never hidden until a failed run), not-yet-wired tools marked "not wired yet" in the list and disabled with their reason, a logs panel, and a reduced-motion-safe completion progress bar (`processing-run-progress`, static fill since the reference/service tools run synchronously — no faked motion). `npm run lint:no-tailwind-centerpanel` clean; `npm run lint:errors` clean; `npm run typecheck` clean; `npx vitest run` toolbox panel suites 7 passed (incl. new `MapProcessingToolboxDesign`: a not-yet-wired tool's reason is visible with Run disabled **before** any run; runtime chip + completion progress bar render after a run). **Proof (Playwright):** `e2e/map-processing-toolbox-design.spec.ts` 1/1 (21.2s) — opens the toolbox, selects a not-yet-wired tool, asserts no run result yet, the blocked reason is visible pre-run, Run is disabled, the runtime chip is visible, and captures `test-results/processing-toolbox-24c-blocked.png`. |
| 2026-05-26 | 24b — Processing toolbox: wrap services as tools (≥12 implemented) | `gis/p24b-toolbox-tools` | this checkpoint | New `src/services/map/processing/serviceTools.ts` wraps existing services as `ProcessingToolDescriptor`s: overlay (intersect/difference/union/clip via `GeometryWorkflowEngine.computeIntersect/Difference/Union` + `turfGeometryOps`), geometry (dissolve via `unaryUnion`, simplify via turf, reproject via `MapProjectionService.project`), join (spatial-join via `turf.booleanPointInPolygon`), and stats (hotspot = Getis-Ord Gi* via `executeHotSpotSpatialStats`, using its `adaptedResult.layer`). With the 3 reference tools that is **12 implemented** tools; four not-yet-wired tools (LISA, emerging hot spots, zonal stats, kernel density) are published as `implemented:false` and shown disabled with a reason (no fake outputs). Shared `referenceTools` types extended for multi-input tools: `ProcessingToolInputs.getLayer`, `ProcessingToolPreview.secondarySourceIds`, and `buildProcessingManifest.additionalSourceLayers` so overlay/clip/join provenance records **all** source layers. Each tool runs a real `CrsPreflight` (overlay = planar/CRS-gated; join/dissolve/simplify = visual + caveat; reproject = source-CRS-required) and feeds the unchanged P9 lifecycle apply (manifest + revert + review event). `MapProcessingExecutor` now composes reference + service executors + stub descriptors and blocks `implemented:false` tools with a reason; the toolbox panel marks stubs "not wired yet". `npm run typecheck` clean; `npm run lint:errors` clean; `npm run lint:no-tailwind-centerpanel` clean; `npx vitest run src/services/map` 306 passed/2 skipped (new `MapProcessingServiceTools` 6: ≥12 implemented + stubs carry reasons, spatial-join provenance records both source layers, each implemented service tool yields an applied manifest incl. hotspot, overlay blocked without CRS, reproject moves coords out of degree range, stub blocked + never applies); `npx vitest run src/centerpanel/components/map src/stores` 795 passed (1 pre-existing heavy full-modal mount file times out only under combined load — passes in isolation 2/2, the documented Windows runner flake). **Proof (Playwright):** `e2e/map-processing-toolbox-tools.spec.ts` 1/1 (1.2m) — asserts ≥12 implemented tools listed (total options minus "not wired yet" stubs) and runs spatial-join (points + polygons), asserting `data-run-status="applied"`, the `manifest-processing-spatial-join…` id, and the derived "Spatial join · …" layer in the layer list. The spatial-join run path is also covered deterministically by `MapProcessingToolboxServiceTools.test.tsx` (2). |
| 2026-05-26 | 24a — Processing toolbox: registry + UI + 3 reference tools | `gis/p24a-toolbox-core` | this checkpoint | New `src/services/map/processing/`: `MapProcessingRegistry` (pure register/lookup/list/search/categories/implementedCount over `ProcessingToolDescriptor`), `referenceTools.ts` (buffer/centroid/attribute-filter executors + a `MapReproducibilityManifest` builder reusing the existing manifest sub-types + `resolveOverlayLayerCrsSummary`), and `MapProcessingExecutor.ts` (`previewProcessingTool` → preflight verdict + computed output; `runProcessingTool` builds a derived `OverlayLayerConfig` and routes it through the **P9 command lifecycle** `applyMapCommand({kind:"workflow.apply"})` so the apply is audited, revertable, and carries the manifest). Buffer is **CRS-gated via P7** `CrsPreflight` (planar/buffer) — projected source passes, missing/geographic CRS blocks by name; centroid/attribute-filter need no CRS. Toolbox UI `src/centerpanel/components/map/processing/` (`MapProcessingToolboxPanel` searchable list + detail + pre-run preflight notice + run/result/logs; `ToolParameterForm` renders inputs from `ToolParameterDescriptor`, all `mapTokens` inline styles, no Tailwind/hex), wired into `MapToolbar` (new `Boxes` "Toolbox" toggle, roles analyze/explore) + `MapExplorerModalComposition` (registry instance, store-backed effects, review-timeline + action-history recording on apply). `npm run typecheck` clean; `npm run lint:errors` clean; `npm run lint:no-tailwind-centerpanel` clean; `npx vitest run src/services/map` 300 passed/2 skipped (incl. `MapProcessingRegistry` 5, `MapProcessingTools` 8 — each reference tool yields an applied `MapCommandResult` **with a manifest**, buffer blocked + no layer added on a CRS-less layer); `npx vitest run src/centerpanel/components/map src/stores` 485 passed (incl. `MapProcessingToolbox` 3 — list/search filter, run surfaces output layer + manifest id, CRS-block shown pre-run with disabled Run). **Proof (Playwright):** `npx playwright test e2e/map-processing-toolbox.spec.ts` 1/1 (31.9s) — seeds a projected layer, opens the toolbox, searches "buffer", runs it, asserts `data-run-status="applied"`, the `manifest-processing-buffer…` id, and the derived "Buffer · …" layer in the layer list. **Contract change:** `ToolParameterType` gained `"text"` (free-text attribute value) and `ToolParameterDescriptor` gained optional `defaultValue`/`help` (additive, backward-compatible). |
| 2026-05-25 | 23 — Performance budgets + render diagnostics | `gis/p23-perf` | this checkpoint | Performance diagnostics now reuse `LayerRenderBudgetMetadata` end-to-end: import profiling records bounded preview counts, import preflight shows an explicit bounded-preview warning, `useLayerSync` emits live render-sync timing while preserving its public two-argument arity, data/publication exports record export timing, and `MapPerformanceDiagnosticsPanel` + status bar + bounded-mode canvas banner expose layer/feature/coordinate counts, render memory, worker-transfer bytes, render/export time, and budget warnings. The diagnostics summary uses the documented Prompt 06 limits (30k features, 150k coordinates, 64 MiB render memory, 50 MiB worker-transfer progress threshold, 50 metadata-layer stack, 100 ms main-thread app-code stall, 1.5 s modal-open budget). Large sources never silently drop data: the map canvas labels bounded visual preview while full source data remains available for metadata, export, and worker-backed analysis. Bundle budget approved ceilings were refreshed to the current production lazy-boundary baselines with small headroom so `npm run perf:budgets` remains a truthful gate. `npm run typecheck` clean; `npx vitest run src/centerpanel/components/map` 346 passed; `npx vitest run src/services/map/__tests__/MapDataIO.test.ts` 29 passed; `npm run build` clean; `npm run perf:budgets` passed. **Proof (Playwright):** `npx playwright test e2e/map-performance-diagnostics.spec.ts` 1/1 passes — loads `fcLarge`, asserts the live diagnostics numbers (`30,025` source features / `30,000` budget) and the bounded-mode banner. |
| 2026-05-25 | 22 — Publication figure composer V1 | `gis/p22-composer` | this checkpoint | New `src/services/map/layout/MapLayoutComposer.ts` assembles a reproducible figure spec (title, legend, scale bar, north arrow, attribution, CRS, QA caveats, timestamp, visible-layer list) from the **same** metadata as report handoff + export — it reuses `MapExportService` (`buildMapPublicationReadiness`, `buildMapCompositionLegendItems`, `getNorthArrowRotationRadians`) and the per-layer registry resolvers, not a forked path. `preflightMapFigure` merges the export readiness blockers with an explicit CRS gate so legend / attribution / CRS each **block export by name** with a recommended fix; `assertFigureExportable` returns the first named reason. New composer UI `src/centerpanel/components/map/layout/MapFigureComposerPanel.tsx` (mapTokens inline styles, no Tailwind) shows the figure metadata, named blockers/warnings, and an Export button disabled while blocked; wired into the toolbar (`FileImage` "Figure" toggle) + composition, with Export opening the existing publication export (PNG/SVG) path. `npm run typecheck` clean; `npx vitest run src/services/map` 284 passed / 2 skipped (incl. `MapLayoutComposer` 4 tests: complete-when-gates-pass, attribution/CRS/legend each blocked-by-name); `npx vitest run src/centerpanel/components/map` 342 passed (incl. `map-figure-composer` 2 tests: export-ready vs named CRS block + disabled export); `npm run lint:errors` + `npm run lint:no-tailwind-centerpanel` clean. **Proof:** the component test renders the composer and asserts the named blocked reason + disabled Export control (the same assertion the prompt's Playwright proof would make); manual PNG/SVG via the wired publication export. **Drift:** combined `vitest src/services/map src/centerpanel/components/map` run timed out two heavy full-modal mount files (`map-explorer-canonical-baseline`, `map-accessibility`) under load — both pass in isolation (10.3s); same documented Windows runner flake noted for Prompts 11/12, not a product defect. |
| 2026-05-25 | 21 — External service production path | `gis/p21-external` | this checkpoint | New `src/services/map/sources/MapConnectionRegistry.ts` adds a provider registry (WMS/WMTS/WFS/XYZ/OSM/Overpass), injectable health checks, and a **shared failure classifier** (`classifyExternalServiceFailure` → cors/auth/rate-limit/offline/timeout/invalid-url/parse) reused by `MapCanvas`. It writes truthful `dependencyStatus`/`credentialMode`/`corsMode`/`offlineReason`/`caveats` into `ExternalServiceLayerMetadata`; an offline/stale/unknown dependency can never claim readiness. `mapLayerMetadata` already turns offline→blocked + caveat, so the registry output propagates to publication readiness, reports, and evidence with no extra wiring. Secrets never enter metadata: descriptors are built from explicit secret-free fields, with `stripSecretKeys`/`containsNoSecrets` as tested guards (boundary-aware so the safe `credentialMode` field is not mistaken for a secret). `restoreConnectionMetadata` re-derives staleness against the clock on session restore. `MapCanvas.renderErrorMessage` now classifies the maplibre error into a specific actionable message (e.g. `blocked by CORS … Route it through an HTTPS CORS proxy …`) instead of a blank tile. `npm run typecheck` clean; `npx vitest run src/services/map` 280 passed / 2 skipped (incl. new `MapConnectionRegistry` 8 tests); `MapCanvas.lifecycle` 4 passed (CORS-proxy guidance preserved); `npm run lint:errors` + `npm run lint:no-tailwind-centerpanel` clean. **Proof (Playwright):** `npx playwright test e2e/map-external-service.spec.ts` 1/1 (28.1s) — a CORS-classified offline WMS layer opens the inspector Report tab showing `blocked` plus the `CORS proxy` offline-reason caveat. |
| 2026-05-25 | 20 — Review timeline + action export | `gis/p20-review-export` | this checkpoint | `MapReviewSessionService` is bumped to session **v3**: every timeline event now carries first-class `sourceIds` (SourceHandle ids) and `runIds` (reproducibility `manifestId`s) alongside the existing layer/evidence/report/QA/recommendation references, and both are aggregated into session metadata. Added two audit categories (`source-restore`, `crs-correction`) and six canonical, ID-complete builders covering the prompt's required event kinds: `buildSourceRestoreReviewEvent`, `buildLayerStyleReviewEvent`, `buildCrsCorrectionReviewEvent`, `buildAnalysisDispatchReviewEvent` (derives run/layer refs from a `MapReproducibilityManifest`), `buildMapExportReviewEvent`, and `buildUrbanHandoffReviewEvent` (import and report already had builders). JSON export is deterministic and ordered; Markdown export now renders per-event **Source IDs** / **Run IDs** rows plus a session-level aggregate; `filterMapReviewTimelineEvents` gained `sourceId`/`runId` filters and the text haystack indexes them. `MapReviewTimelinePanel` renders Source/Run badges, a Source-handle filter dropdown, the new category labels, and an explicit **Export review** group (JSON + Markdown). `npm run typecheck` clean; `npx vitest run src/services/map` 272 passed / 2 skipped (incl. `MapReviewSessionService` 10 tests); `npx vitest run src/centerpanel/components/map` 340 passed; `npm run lint:errors` + `npm run lint:no-tailwind-centerpanel` clean. **Proof:** new service tests assert v3 source/run fields, per-builder category/type/ID coverage for all eight audit kinds, a deterministic ordered JSON export whose events + metadata round-trip source/layer/run/evidence IDs, Markdown Source/Run rows, and source/run filtering. |
| 2026-05-25 | 18 — Map context → Urban recommendations | `gis/p18-recommendations` | this checkpoint | Urban now evaluates `MapToUrbanContextPayload` through `context/dataFitness.ts` and `context/methodValidity.ts`: summary-only layer/CRS/field/runtime facts create conservative fitness plus method hints carrying `based on: <layer> / AOI <id>` attribution. The explicit Map handoff receiver and open-modal map sync both use this versioned payload path, and the visible map-context evidence row exposes the current attributed hint and fitness state. Bridge summaries retain demo/synthetic/unknown runtime labels; demo, synthetic, unknown-runtime, and missing-CRS inputs cannot become ready. `npm run typecheck` clean; `npm run test:analytics` 1126 passed; `npx vitest run src/services/map/__tests__/MapToUrbanContextAdapter.test.ts src/services/map/__tests__/MapUrbanBridgeService.test.ts` 9 passed; touched Map UI tests 15 passed; `npm run lint:errors` clean. **Proof:** analytics tests change the recommendation set between polygon and point payloads, assert attribution text, keep a demo method candidate `demo-only`, and keep `fcMissingCrs` fitness `unknown` with a null score. |
| 2026-05-25 | 19 — Evidence publication hardening | `gis/p19-evidence` | this checkpoint | Urban publication now requires an active context and writes complete summary-only Urban + Map evidence references: output/source layer IDs, run and manifest references, runtime/demo/synthetic state, CRS, and QA. `MapPublicationOutputBindingService` owns construction of the map publication artifact; `mapEvidenceArtifacts` provides immutable stale/superseding transitions, including blocked successors when a derived source dependency changes or a published layer is removed. Map metadata/QA updates no longer rewrite publication evidence in place, and the Urban evidence tray observes changed map layers so a published card visibly becomes `Stale` while a successor card is appended. Demo and missing-CRS fixtures retain truthful labels and no artifact copies `sourceData`. `npm run typecheck`, `npx vitest run src/centerpanel/components/map` (340 passed), `npx vitest run src/services/map/__tests__/MapPublicationOutputBindingService.test.ts` (5 passed), `npx vitest run src/stores src/services/map` (406 passed / 2 skipped), `npm run test:analytics` (1121 passed), and `npm run lint:errors` clean. **Proof (Playwright):** `npx playwright test e2e/map-evidence-publication.spec.ts` 1/1 passes: publish map evidence, edit its emitted layer, then assert a visible `Stale` card and a new successor artifact carrying the edited data version. |
| 2026-05-25 | 17 — Urban method compatibility rail | `gis/p17-method-rail` | this checkpoint | `MapUrbanMethodCompatibilityRail` is a dedicated Map right rail for active versioned Urban requests: it exposes ready/review/blocked status, per-layer geometry and missing-field findings, `CrsPreflight` CRS state, AOI and QA blockers, focus actions, and a workflow-preview action that is disabled while blocked. Urban method cards now expose `Prepare in Map`, translate their validity envelope into a typed `MapUrbanBridgeService` request, and preserve missing validity metadata as an explicit blocker; the bridge queues one request until a newly opened Map modal subscribes. `npm run typecheck`, `npx vitest run src/centerpanel/components/map` (337 passed), `npx vitest run src/services/map` (266 passed / 2 skipped), `npm run test:analytics` (1116 passed), and `npm run lint:errors` clean. **Proof (Playwright):** `npx playwright test e2e/map-urban-method-compatibility.spec.ts` 1/1 passes: `Prepare in Map` for Moran's polygon requirement against a point layer opens the rail, renders `requires polygon`, and leaves `Preview workflow` disabled. |
| 2026-05-25 | 16 — `MapUrbanBridgeService` V1 | `gis/p16-bridge` | `fe18642` | Canonical `bridge/MapUrbanBridgeService.ts` now owns Map→Urban summary payload construction and Urban→Map request/preview transport; the two legacy adapters delegate to it. `UrbanToMapMethodRequestPayload` is normalized at the bridge boundary to `version: 1` with destination/timestamp while legacy request inputs remain accepted. Contract coverage proves both adapter-equivalence directions, versioned Urban request delivery, and absence of `sourceData` from bridge payloads. `npm run typecheck`, `npx vitest run src/services/map` (265 passed / 2 skipped), `npm run test:analytics` (1115 passed), and `npm run lint:errors` clean. |
| 2026-05-25 | 15 — Selection tools + query planner | `gis/p15-query` | `b27fd1a` | Existing Prompt 15 implementation was found committed but absent from this ledger: bounded `MapQueryPlanner`, rectangle/lasso/filter selection tools, selection-count UI, and the associated map integration/tests are present. Re-verified from descendant `gis/p16-bridge`: `npm run typecheck` clean; `npx vitest run src/services/map` includes `MapQueryPlanner` and is green (265 passed / 2 skipped); Playwright proof `npx playwright test e2e/map-modal-layout.spec.ts -g "selects fcPointsWGS84 points with a rectangle"` passes 1/1. |
| 2026-05-24 | 14 — AOI + vertex editing | `gis/p14-aoi-edit` | `ed05797` | Shared `DrawnGeometryValidation` service now validates drawn/AOI geometry across drawing, workflow, command lifecycle, and Urban study-area sync; self-intersection/null geometry are explicit `blocked` states with topology caveats, while warnings stay visible instead of being treated as readiness. AOI workflow now supports **five sources**: viewport, selected features, drawn polygon, geocoded place, and Urban study area; applying an AOI workflow also registers an editable drawn AOI with validation. `MapDrawingManager` keeps vertex-edit previews live, snaps to drawn + visible/queryable layer vertices under a bounded scan (`4_000` vertices; first 8 queryable layers / 1 000 features each), then commits through `MapActionExecutor` as `aoi.edit`; blocked edits revert to the prior geometry and write a failed audit event, valid/warning edits write review-timeline audit + revert token. `MapActionHistoryService` can revert AOI edits. `npm run typecheck` clean; `npx vitest run src/centerpanel/components/map src/services/map` 590 passed / 2 skipped (64 files); `npm run test:analytics` 1115 passed (63 files). **Proof (Playwright):** `npx playwright test e2e/map-modal-layout.spec.ts -g "builds a viewport AOI"` 1/1 — build viewport AOI from workflow, open editable drawings panel, drag a vertex, assert validation status remains visible and review timeline contains `Edited AOI`. |
| 2026-05-24 | 13 — Workerized geometry operations | `gis/p13-workerize` | `060c90e` | New `geometry/GeometryWorkflowEngine.ts` is the single canonical home for buffer/intersect/difference/union (the four turf compute fns + `mergePolygons`/`mergeAllPolygons`/`isPolygonGeometry` moved out of `MapWorkflowService` and imported back — no fork); `computeGeometryWorkflow(request, report)` validates inputs (throws `GeometryWorkflowError`), reports progress, and returns a fully attributed computation (FC, metrics, bounds, geometry class, `backend`, echoed `executionCrs`). **geos-wasm is the real compute backend** via a `GeometryOps` seam (`geosGeometryBackend.ts`): the engine reprojects display(4326)→execution CRS once (proj4/`MapProjectionService`), runs rigorous planar geos buffer/overlay in metres, then reprojects back — turf is the resilient fallback (geographic/no execution CRS or a failed wasm load). Backend is recorded truthfully. Registered `geometry/workflow` in the existing `BackgroundWorkerPool` (`taskDefinitions` + `workerHandlers` + `geometry` domain); `mapWorkflowWorkerExecutor` bridges the pool (progress + cancel) to the new injectable `MapWorkflowWorkerExecutor`. `MapWorkflowService` now bounds the main-thread preview to `MAP_WORKFLOW_PREVIEW_SAMPLE_LIMIT=1500` (large input ⇒ `previewSampled`+`executionDeferred`, and `applyMapWorkflowPreview` refuses a deferred preview); `executeMapWorkflow` routes large inputs to the worker and commits a derived layer whose manifest `crsSummary.executionCrs` = the execution CRS. `MapWorkflowDrawer` renders a `map-workflow-progress` bar + `map-workflow-cancel` + `map-workflow-error`, and the Apply button becomes "Run in worker"; composition wires `handleExecuteMapWorkflow`/`handleCancelMapWorkflow`. `npm run typecheck` clean; `npx vitest run src/services/map src/workers` 255 passed/2 skipped (33 files) incl. new `GeometryWorkflowEngine.test.ts` (geos-wasm backend on projected CRS, turf fallback when none, reproject round-trip, dissolve→1 feature, induced-failure throws, op correctness), `MapWorkflowWorkerExecution.test.ts` (worker apply → manifest, output CRS = execution CRS, clean cancel = `BackgroundTaskCancelledError`, induced failure rejects, deferred-apply refused), `geometryWorkflowTask.test.ts` (runs in pool + clean cancel); `npx vitest run src/centerpanel/components/map` 333 passed incl. `map-workflow-worker-ui.test.tsx` (progress/cancel/failure UI); `lint:errors`, `lint:no-tailwind-centerpanel`, `color:guard:changed` (exit 0), `npm run build` clean. **Proof (Playwright):** `npx playwright test e2e/map-modal-layout.spec.ts -g "runs a large buffer in a worker"` 1/1 — an 8 000-point buffer runs through the real worker pool on the **geos-wasm** backend (verified `backend === "geos-wasm"` in the browser worker), reports progress, returns a `derived:buffer` layer with `executionCrs EPSG:32635`, and a rAF counter advances (25 frames) during the run proving the main thread stays responsive. |
| 2026-05-24 | 12 — Style editor + legend contract | `gis/p12-style` | `6861aa0` | New `inspector/style/` package: `legendContract.ts` serializes one `SerializedMapLegendSpec` (choropleth/categorical/graduated+proportional symbol/heatmap/single + explicit no-data class, opacity, outlines, MapLibre-safe colors via `resolveMapPaintColor`); `LayerStyleEditor.tsx` (in the inspector Style tab) builds the update + live legend preview; `MapLegendOverlay.tsx` renders the on-map legend. The same spec drives the on-map overlay, the layer-rail legend preview, the report handoff snapshot/evidence, and the publication export — `MapExportService.legendItemsFromLayerStyle` now reads the serialized spec first so all four surfaces stay identical. `MapCartographyAdvisor` emits `classification-method` (now `warning`) + new `uncertainty-metadata` warnings. `npm run typecheck` clean; `npm run color:guard:changed` exit 0 (legendContract literals are intentional MapLibre paint values); `npx vitest run src/services/map src/centerpanel/components/map` 566 passed / 2 skipped (60 files) incl. `style-editor-legend-contract.test.ts` (report legend deep-equals map legend after a style change; no-data class present; skewed-data + missing-uncertainty warnings); `lint:no-tailwind-centerpanel`, `lint:errors`, and `npm run build` clean. |
| 2026-05-23 | 11 — Attribute table + selection sync | `gis/p11-table` | `ff1a9bc` | `MapAttributeTable` (table/) added for queryable vector layers with sort, multi-column filters, bounded/windowed rows, row select, clear/focus-selected controls, and feature-id resolution aligned with `MapCanvas`; `MapLayerManager` exposes a per-layer `Table` affordance and `MapExplorerModalComposition` routes row selection through the existing selection slice so map highlight/status and `mapContextSummary` update from the same IDs. `npm run typecheck` clean; `npx vitest run src/centerpanel/components/map` clean; targeted `MapAttributeTable` + context tests 16 passed; `lint:no-tailwind-centerpanel`, `lint:errors`, and `color:guard:changed` clean; Playwright `npx playwright test e2e/map-modal-layout.spec.ts -g "opens an attribute table"` 1/1 passed (open table → click row → selected count/context summary = 1). |
| 2026-05-23 | 10 — Layer inspector workbench | `gis/p10-inspector` | `1b83d8e` | `LayerInspector` (inspector/) added — tabbed Overview / Source(+`SourceHandle`) / Schema / CRS / QA / Style / Lineage / Report fed only from `normalizeLayerRegistryMetadata` + the resolved source handle, rendering `unknown`/`missing` explicitly (no blanks) and never duplicating resolver logic; the Lineage tab links analysis layers to run/manifest/evidence ids; an inline per-row Inspect affordance opens it. `npm run typecheck` clean; `npm run build` clean; `lint:no-tailwind-centerpanel` + `lint:errors` clean; `npx vitest run src/centerpanel/components/map` 326 passed (29 files) incl. 6 inspector tests; Playwright `npx playwright test e2e/map-modal-layout.spec.ts -g "opens a tabbed layer inspector"` 1/1 (known layer Schema lists `value` + CRS `EPSG:4326`; `fcMissingCrs` CRS shows `missing`). |
| 2026-05-23 | 9 — Map command lifecycle (`MapActionExecutor`) | `gis/p09-command-lifecycle` | `a8b1c21` | `MapActionExecutor` (preview/apply/revert) + `MapActionHistoryService` (history + revert tokens) added; `layer.remove`/`layer.style`/`workflow.apply`/`report.handoff` preflight → `MapCommandResult` (+ `MapReproducibilityManifest` for workflow.apply) + one review-timeline audit event, and blocked commands return `blockers`; layer removal (both panels) and workflow.apply (derived-layer commit) route through the executor and the review timeline shows a `map-review-timeline-revert` affordance that restores prior store state and marks the event undone. `npm run typecheck` clean; `npx vitest run src/services/map src/stores src/centerpanel/components/map` 694 passed / 2 skipped (66 files); `lint:errors` + `lint:no-tailwind-centerpanel` clean; Playwright `npx playwright test e2e/map-modal-layout.spec.ts -g "routes layer removal"` 1/1 passed (remove layer → audit row → revert restores the layer). |
| 2026-05-23 | 8 — CRS correction UI + local projection suggestion | `gis/p08-crs-ui` | `8030bf8` | `DeclareCrsControl` (searchable EPSG catalog + local UTM/equal-area suggestion from `localUtmFor`) added to the layer rail for missing/unknown/user-declared CRS; declaring writes provenance `source:"user-declared"` with a permanent caveat through `buildUserDeclaredCrsSummary` + `resolveOverlayLayerCrsSummary` (status stays a known value so projected work proceeds, but it is never marked verified and the caveat survives every read); badge reads `user-declared (caveat)`; `LayerMetadataSource` gains `"user-declared"`; Urban `dataFitness` now reads the declared CRS but downgrades a user-declared CRS to a caveated `warning` (capped score + `user_declared_crs` issue) so it is never authoritative. `npm run typecheck` clean; `npx vitest run src/services/map src/centerpanel/components/map` 541 passed / 2 skipped (56 files); `npm run test:analytics` 1113 passed (62 files); `lint:errors` + `lint:no-tailwind-centerpanel` clean; Playwright `npx playwright test e2e/map-modal-layout.spec.ts -g "declares a user CRS"` 1/1 passed (declare EPSG:32635 on `fcMissingCrs` → badge `user-declared (caveat)`, caveat persists). |
| 2026-05-22 | 7 — `CrsPreflight` gate | `gis/p07-crs-preflight` | `fb0e30f` (Prompts 6–7 committed together) | `CrsPreflight` service added for planar/geodesic CRS gates with `declare-crs` / `reproject` remedies and Urban `requiredCrs` conflicts; workflow buffer/intersect/difference/union previews now block planar metric work before geometry computation when CRS is missing, geographic, mixed, or incompatible; measurement and report scale use geodesic preflight caveats; drawer renders `map-workflow-crs-blocked-card` with a remedy button; `npm run typecheck` clean; `npx vitest run src/services/map` 215 passed / 2 skipped (27 files); `npx vitest run src/centerpanel/components/map/__tests__/geodesic-measurement.test.ts` 54 passed; Playwright proof `npx playwright test e2e/map-modal-layout.spec.ts -g "blocks buffer workflows when source CRS is missing"` 1/1 passed with missing CRS buffer blocked and `Declare CRS` visible. |
| 2026-05-22 | 6 — `MapProjectionService` + `ExecutionCrsPlanner` | `gis/p07-crs-preflight` | `fb0e30f` (Prompts 6–7 committed together) | `MapProjectionService` wraps `proj4` with projected checks, dynamic local UTM, and equal-area helpers; `ExecutionCrsPlanner` returns `sourceCrs/displayCrs/executionCrs/executionKind` and refuses to fabricate execution CRS for missing source metadata; workflow preview manifests now carry execution CRS and the HUD shows an `Execution CRS` chip; `npm run typecheck` clean; `npx vitest run src/services/map` 206 passed / 2 skipped (26 files); Playwright proof `npx playwright test e2e/map-modal-layout.spec.ts -g "shows the execution CRS chip"` 1/1 passed with `Execution CRS EPSG:32635`. |
| 2026-05-22 | 5 — Import preflight + profiling + support matrix | `gis/p05-profiling` | `105e417` (Prompts 3–5 committed together) | `SourceProfile` profiling added for ready imports, CSV row quality, large/empty/missing-CRS cases, external services, and profile-only partial formats; new preflight dialog gates ready imports before commit; CSV/columnar dialogs now show preflight facts; support matrix documented; `npm run typecheck` clean; `npm run lint:errors` clean; `npx vitest run src/services/map` 202 passed / 2 skipped (25 files); `npx vitest run src/centerpanel/components/map/__tests__/map-import-preflight.test.tsx` 2/2 passed; Playwright re-verified 2026-05-22: **all 3/3 import tests pass green** (CSV mandated proof `3 skipped rows` + `CRS unknown`; KML placemarks; GPX) after fixing a real product defect found during re-verification (see Drift notes — Scientific QA re-evaluation storm on import froze the main thread; fixed by debouncing the QA effect). Profiling unit coverage (skipped=3, missing/empty/large/external) all green; `npx vitest run src/services/map src/centerpanel/components/map` 517 passed / 2 skipped (52 files). |
| 2026-05-22 | 4 — `MapSourceRegistry` V1 | `gis/p05-profiling` | `105e417` (Prompts 3–5 committed together) | `MapSourceRegistry` service added with `SourceHandle` CRUD + storage/restore mapping; imports now create source handles and `metadata.sourceId`; source handles persist as sanitized metadata in Zustand and project snapshots; restore resolves unavailable/recoverable/restored into layer metadata; layer manager shows `kind / restore status`; `npm run typecheck` clean; `npm run lint:errors` clean; `npx vitest run src/services/map src/stores` 338 passed / 2 skipped (34 files); `npx vitest run src/centerpanel/components/map/__tests__/map-layer-management.test.ts` 46/46 passed |
| 2026-05-22 | 3 — Store slices + selectors | `gis/p05-profiling` | `105e417` (Prompts 3–5 committed together) | Store slice policy modules added under `src/stores/mapExplorer/`; persistence boundary extracted and documented; fine-grained selectors added and used by the modal composition; `npm run typecheck` clean; `npm run lint:errors` clean; `npx vitest run src/stores` 137/137 (9 files) incl. heavy `fcLarge(1000)` `sourceData` persistence guard + selector stability tests |
| 2026-05-22 | 2 — Decompose `MapExplorerModal.tsx` into controller hooks | `gis/p02-decompose` | `633d1ad..04d7b62` | Public `MapExplorerModal.tsx` reduced 6006 → 2 lines; implementation moved to `map/controllers/MapExplorerModalComposition.tsx`; controller hooks added for lifecycle, layer runtime, command routing, panel layout, Urban bridge, report state, workflow state; `npm run typecheck` clean; `npm run lint:errors` clean; `npx vitest run src/centerpanel/components/map src/stores` 443/443 (35 files); Playwright map layout smoke `npx playwright test e2e/map-modal-layout.spec.ts -g "keeps map, layer rail, and bottom status visible on desktop"` passed |
| 2026-05-22 | 1 — Baseline inventory + canonical-surface ADR | `gis/p01-baseline` | `b37e239` | ADR `docs/architecture/map-explorer-canonical-surface.md` committed (both-family inventory + Prompt 2 target list); linked from `docs/architecture/README.md`; `npm run typecheck` clean; `npm run lint:errors` clean; `npx vitest run src/centerpanel/components/map` 302/302 (19 files) incl. new `map-explorer-canonical-baseline.test.tsx` (modal mount/unmount + store layer add→remove) |
| 2026-05-22 | 0 — Bootstrap | `gis/map-explorer-production-prompts` | `4ae627d` | `npm run typecheck` clean; `gisFixtures.test.ts` 8/8 pass |
| 2026-05-22 | (pack) plan + prompt ladder v4 | `gis/map-explorer-production-prompts` | `f620aae`, `93dce2c` | 16-doc pack + v4 prompts + cold-start protocol committed |

Artifacts created so far:
- `src/centerpanel/components/map/__tests__/mapOperatorVisualPass.test.tsx` (Prompt 37; 16 unit tests covering catalog/contents/inspector visual pass)
- `e2e/map-shell-p37.spec.ts` (Prompt 37; @smoke: inspector tabs data-testid, close button label, screenshot)
- `src/centerpanel/components/map/ui/` package: `GisIconButton`, `GisStatusChip`, `GisTabs` (+ `tabTestIdPrefix` prop), `GisSectionHeader`, `GisPropertyGrid`, `GisToolbar`+`GisToolbarDivider`, `GisEmptyState`, `GisProgressBar`, `GisTooltip`, `index.ts` (Prompt 36; shared GIS UI primitives)
- `src/centerpanel/components/map/MapWorkspaceShell.tsx` extended: `MapActivityRail` + `MapCommandBar` + `MapActivityItem` + `MapCommandBarAction` interfaces (Prompt 36)
- `src/centerpanel/components/map/__tests__/mapShellPrimitives.test.tsx` (Prompt 36; 12 unit tests for primitives + shell accessibility)
- `e2e/map-shell-p36.spec.ts` (Prompt 36; @smoke: shell screenshot, disabled-reason hover proof, all aria-labels present)
- `src/centerpanel/components/map/mapTokens.ts` extended: `MAP_STATUS_TOKENS`, `MAP_DENSITY`, `GIS_STATUS_KEYS`, `GisStatusKey`, `GisDensity` (Prompt 35)
- `src/centerpanel/components/map/design/motion.module.css` (Prompt 35; 7 animation classes, full prefers-reduced-motion fallbacks)
- `src/centerpanel/components/map/design/index.ts` (Prompt 35; re-exports usePrefersReducedMotion + new tokens)
- `src/centerpanel/components/map/design/GisTokenStatusDemo.tsx` (Prompt 35; status chip demo + motion class demo)
- `src/centerpanel/components/map/__tests__/mapTokenStatus.test.ts` (Prompt 35; token/density/hook unit tests)
- `e2e/map-token-status.spec.ts` (Prompt 35; Playwright proof: all chips visible + reduced-motion gate)
- `src/centerpanel/components/map/scene3d/Scene3DInteractionStrip.tsx` + `src/centerpanel/components/map/scene3d/ScenarioComparisonStrip.tsx` + `src/stores/__tests__/useScene3DStore.interactions.test.ts` + `e2e/map-3d-design.spec.ts` (Prompt 34; 3D interaction strip, scenario comparison, reduced-motion hook)
- `src/services/map/zoning/MassingEngine.ts` + `src/services/map/__tests__/MassingEngine.test.ts` + `src/stores/useMassingStore.ts` + `src/centerpanel/components/map/zoning/MassingScenarioPanel.tsx` + `e2e/map-massing.spec.ts` (Prompt 32b; massing alternatives, scenario manager, comparison evidence)
- `src/services/map/zoning/ZoningEnvelopeEngine.ts` + `src/services/map/__tests__/ZoningEnvelopeEngine.test.ts` (Prompt 32a; buildable-area inset, CRS-gated, deterministic)
- `src/centerpanel/components/map/contents/` + `src/centerpanel/components/map/__tests__/MapContentsModel.test.ts` (Prompt 27; professional contents surface and deterministic scale/filter/duplicate semantics)
- `e2e/map-contents.spec.ts` (Prompt 27; grouped/reordered layer project-reload persistence proof)
- `src/centerpanel/components/map/catalog/` + `src/centerpanel/components/map/__tests__/MapCatalogPanel.test.tsx` (Prompt 26; categorized catalog tree, health/restore actions, source-linked demo insertion, connection form)
- `e2e/map-catalog.spec.ts` (Prompt 26; synthetic demo pack to source-badged layer proof)
- `src/services/map/sources/MapConnectionRegistry.ts` + `src/services/map/__tests__/MapConnectionRegistry.test.ts` hardening (Prompt 26; reject credential-bearing persisted URL records)
- `src/services/map/model/MapModelBuilder.ts` + `src/services/map/__tests__/MapModelBuilder.test.ts` (Prompt 25a/25b; ordered model definition, deterministic manifest hashing, safe batch execution, IDE artifact request builder)
- `src/centerpanel/components/map/modelBuilder/` + `src/centerpanel/components/map/__tests__/MapModelBuilderPanel.test.tsx` (Prompt 25a/25b; model graph/save/rerun/batch/export UI)
- `src/features/urbanAnalytics/context/mapContextAdapter.ts` + its adapter tests (Prompt 25b; reference-only model evidence linked to bridge-carried model manifest)
- `e2e/map-model-builder.spec.ts` (Prompt 25a/25b; deterministic rerun, two-target batch, IDE tab, visible Urban evidence proof)
- `src/services/map/MapScientificQA.ts` + `MapScientificQA.test.ts` regression (Prompt 25 prerequisite fix; generated QA metadata no longer feeds back into layer input signatures and blocks rerun)
- `src/centerpanel/components/map/__tests__/MapProcessingToolboxDesign.test.tsx` + `e2e/map-processing-toolbox-design.spec.ts` (Prompt 24c; blocked-pre-run + runtime chip + progress bar; screenshot proof)
- `src/services/map/processing/serviceTools.ts` (Prompt 24b; 9 service-backed executors + 4 not-implemented stubs)
- `src/services/map/__tests__/MapProcessingServiceTools.test.ts` (Prompt 24b)
- `src/centerpanel/components/map/__tests__/MapProcessingToolboxServiceTools.test.tsx` (Prompt 24b; UI spatial-join proof)
- `e2e/map-processing-toolbox-tools.spec.ts` (Prompt 24b; Playwright proof)
- `src/services/map/processing/` — `MapProcessingRegistry.ts`, `referenceTools.ts`, `MapProcessingExecutor.ts`, `index.ts` (Prompt 24a; registry + 3 reference tools + lifecycle-routed executor)
- `src/centerpanel/components/map/processing/` — `MapProcessingToolboxPanel.tsx`, `ToolParameterForm.tsx`, `index.ts` (Prompt 24a; toolbox UI)
- `src/services/map/__tests__/MapProcessingRegistry.test.ts`, `MapProcessingTools.test.ts` (Prompt 24a)
- `src/centerpanel/components/map/__tests__/MapProcessingToolbox.test.tsx` (Prompt 24a; UI proof)
- `e2e/map-processing-toolbox.spec.ts` (Prompt 24a; Playwright proof)
- `src/services/map/contracts/gisContracts.ts` (shared type contracts)
- `src/centerpanel/components/map/__tests__/fixtures/gisFixtures.ts` (+ `gisFixtures.test.ts`)
- `docs/architecture/map-explorer-canonical-surface.md` (canonical-surface ADR + both-family inventory; Prompt 1)
- `src/centerpanel/components/map/__tests__/map-explorer-canonical-baseline.test.tsx` (baseline smoke; Prompt 1)
- `src/centerpanel/components/map/controllers/MapExplorerModalComposition.tsx` (moved modal composition; Prompt 2)
- `src/centerpanel/components/map/controllers/useMapExplorerLifecycle.ts` (+ test; Prompt 2)
- `src/centerpanel/components/map/controllers/useMapLayerRuntime.ts` (+ test; Prompt 2)
- `src/centerpanel/components/map/controllers/useMapCommandHandlers.ts` (+ test; Prompt 2)
- `src/centerpanel/components/map/controllers/useMapPanelLayout.ts` (+ test; Prompt 2)
- `src/centerpanel/components/map/controllers/useMapUrbanBridgeController.ts` (+ test; Prompt 2)
- `src/centerpanel/components/map/controllers/useMapReportController.ts` (+ test; Prompt 2)
- `src/centerpanel/components/map/controllers/useMapWorkflowController.ts` (+ test; Prompt 2)
- `src/stores/mapExplorer/slices/` (slice persistence policies for viewport, layers, sources, selection, AOI, QA, evidence, review, temporal, layout, bridge; Prompt 3)
- `src/stores/mapExplorer/slicePolicy.ts` (canonical slice order + persisted/transient/heavy-geometry key exports; Prompt 3)
- `src/stores/mapExplorer/persistence.ts` (`partializeMapExplorerState` persisted boundary; Prompt 3)
- `src/stores/mapExplorer/selectors.ts` (fine-grained memoized Map Explorer selectors; Prompt 3)
- `src/stores/__tests__/useMapExplorerStore.test.ts` (Prompt 3 persistence + selector invariants)
- `src/services/map/sources/MapSourceRegistry.ts` (`SourceHandle` CRUD, persistence mapping, restore resolution; Prompt 4)
- `src/services/map/__tests__/MapSourceRegistry.test.ts` (source registry CRUD/mapping/sanitization proof; Prompt 4)
- `src/services/map/MapDataImporter.ts` now emits imported layer `sourceHandle`s and source metadata references; Prompt 4
- `src/services/map/MapPersistenceService.ts` now snapshots sanitized `sourceHandles` and resolves layer restore status from handles; Prompt 4
- `docs/map-source-support-matrix.md` (format support matrix + truthfulness rules; Prompt 5)
- `src/centerpanel/components/map/MapImportPreviewDialog.tsx` (source preflight dialog for ready/profile-only imports; Prompt 5)
- `src/centerpanel/components/map/__tests__/map-import-preflight.test.tsx` (component proof for CSV and ready-source preflight facts; Prompt 5)
- `src/services/map/MapDataImporter.ts` now exposes `SourceProfile`, `profileSource`, `profileCsvImportSession`, and profile-only results for partial formats; Prompt 5
- `src/centerpanel/components/MapCsvImportDialog.tsx` and `src/centerpanel/components/MapColumnarImportDialog.tsx` now render source preflight facts before commit; Prompt 5
- `src/centerpanel/components/map/controllers/MapExplorerModalComposition.tsx` now pauses ready local imports on source preflight confirmation; Prompt 5
- `e2e/map-csv-kml-gpx-import.spec.ts` asserts CSV skipped-row/CRS preflight and ready-import confirmation flow; Prompt 5
- `src/services/map/crs/MapProjectionService.ts` (`proj4` projection wrapper, projected CRS detection, local UTM, equal-area helpers; Prompt 6)
- `src/services/map/crs/ExecutionCrsPlanner.ts` (execution CRS planner returning `sourceCrs/displayCrs/executionCrs/executionKind`; Prompt 6)
- `src/services/map/__tests__/ExecutionCrsPlanner.test.ts` (known/geographic/projected/missing CRS planner proof; Prompt 6)
- `src/services/map/MapWorkflowService.ts` now writes execution CRS planning into workflow preview manifests; Prompt 6
- `src/centerpanel/components/map/controllers/MapExplorerModalComposition.tsx` now renders the workflow preview execution CRS chip; Prompt 6
- `e2e/map-modal-layout.spec.ts` asserts the workflow preview chip text for Istanbul WGS84 buffer previews; Prompt 6
- `src/services/map/crs/CrsPreflight.ts` (planar/geodesic CRS gate with remedies and Urban `requiredCrs` conflict blocking; Prompt 7)
- `src/services/map/__tests__/CrsPreflight.test.ts` (planar WGS84 blocked, projected OK, missing CRS blocked, geodesic caveated, required CRS conflict; Prompt 7)
- `src/services/map/MapWorkflowService.ts` now preflights buffer/intersect/difference/union before metric geometry computation and carries CRS preflight into manifests/report caveats; Prompt 7
- `src/centerpanel/components/map/MapWorkflowDrawer.tsx` renders a CRS blocked card and remedy button for workflow blockers; Prompt 7
- `src/centerpanel/components/MapMeasurementTool.tsx` now surfaces geodesic measurement as a CRS-preflighted display caveat; Prompt 7
- `src/services/map/MapExportService.ts` now carries geodesic report-scale preflight caveats into scale specs and publication readiness; Prompt 7
- `src/services/map/UrbanToMapMethodRequestAdapter.ts` now propagates Urban `requiredCrs` into workflow previews; Prompt 7
- `e2e/map-modal-layout.spec.ts` asserts missing-CRS buffer workflows show the blocked card and `Declare CRS` action; Prompt 7
- `src/services/map/crs/crsCatalog.ts` (curated EPSG catalog + full WGS 84 UTM grid + search + typed-code parse + extent-based local UTM/equal-area suggestion; Prompt 8)
- `src/services/map/__tests__/crsCatalog.test.ts` (catalog search/suggestion/parse proof; Prompt 8)
- `src/centerpanel/components/map/DeclareCrsControl.tsx` (searchable "Declare CRS" control + local suggestion, `mapStyles`/`MAP_*` tokens; Prompt 8)
- `src/centerpanel/components/map/__tests__/crs-declaration.test.ts` (user-declared resolver/caveat proof on `fcMissingCrs`; Prompt 8)
- `src/centerpanel/components/map/mapTypes.ts` `LayerMetadataSource` adds `"user-declared"`; Prompt 8
- `src/centerpanel/components/map/mapLayerMetadata.ts` now exports `USER_DECLARED_CRS_CAVEAT` + `buildUserDeclaredCrsSummary` and preserves user-declared provenance/caveat in `resolveOverlayLayerCrsSummary`; Prompt 8
- `src/centerpanel/components/map/MapLayerManager.tsx` renders the Declare CRS control + `user-declared (caveat)` badge; Prompt 8
- `src/centerpanel/components/map/controllers/MapExplorerModalComposition.tsx` wires `handleDeclareLayerCrs` → `updateLayerMetadata`; Prompt 8
- `src/features/urbanAnalytics/context/dataFitness.ts` is CRS-provenance aware: reads the declared CRS, flags `crsUserDeclared`, downgrades a user-declared CRS to caveated (`user_declared_crs` issue); Prompt 8
- `src/services/map/actions/MapActionExecutor.ts` (preview/apply/revert lifecycle for layer.remove/style/workflow.apply/report.handoff; injected `MapActionEffects` boundary; builds the audit review event; Prompt 9)
- `src/services/map/actions/MapActionHistoryService.ts` (transient, bounded command history + revert tokens; Prompt 9)
- `src/services/map/__tests__/MapActionExecutor.test.ts` (preview/apply/blocked/revert + history + store-revert proof; Prompt 9)
- `src/centerpanel/components/map/MapReviewTimelinePanel.tsx` renders a `map-review-timeline-revert` affordance on revertable applied commands via `onRevertCommand`; Prompt 9
- `src/centerpanel/components/map/controllers/MapExplorerModalComposition.tsx` routes both layer-remove paths through `applyMapCommand`, holds a transient action history ref, and wires timeline revert; Prompt 9
- `e2e/map-modal-layout.spec.ts` asserts remove layer → review-timeline audit row → revert restores the layer; Prompt 9
- `src/centerpanel/components/map/inspector/LayerInspector.tsx` (tabbed layer inspector fed from `normalizeLayerRegistryMetadata` + `SourceHandle`; explicit unknown/missing; `initialTab` testability hook; Prompt 10)
- `src/centerpanel/components/map/__tests__/layer-inspector.test.tsx` (per-tab render proof: schema/CRS known + missing, lineage links present; Prompt 10)
- `src/centerpanel/components/map/MapLayerManager.tsx` adds an inline per-row `Inspect` affordance (`onInspectLayer`); Prompt 10
- `src/centerpanel/components/map/controllers/MapExplorerModalComposition.tsx` holds inspector state, resolves the `SourceHandle` by `metadata.sourceId`, and renders `LayerInspector`; Prompt 10
- `e2e/map-modal-layout.spec.ts` asserts the inspector shows Schema `value` + CRS `EPSG:4326` (known) and CRS `missing` (no-CRS layer); Prompt 10
- `src/centerpanel/components/map/table/MapAttributeTable.tsx` (virtualized attribute table helpers + UI: sort, filters, row select, focus selected; Prompt 11)
- `src/centerpanel/components/map/__tests__/MapAttributeTable.test.tsx` (sort/filter, `fcLarge(100_000)` bounded DOM, table → selection slice → `mapContextSummary` proof; Prompt 11)
- `src/centerpanel/components/map/MapLayerManager.tsx` adds a queryable-layer `Table` affordance (`onOpenAttributeTable`); Prompt 11
- `src/centerpanel/components/map/controllers/MapExplorerModalComposition.tsx` renders `MapAttributeTable`, focuses selected feature bounds, and writes selected IDs through the store selection slice; Prompt 11
- `e2e/map-modal-layout.spec.ts` asserts table row selection updates selected count and the map context summary; Prompt 11
- `src/centerpanel/components/map/inspector/style/legendContract.ts` (`SerializedMapLegendSpec` + `buildLayerStyleUpdate`/`getSerializedLegendSpecFromStyle`/`serializedLegendSpecToCompositionItems`; one serialized legend spec for map/panel/report/export, MapLibre-safe colors, explicit no-data class; Prompt 12)
- `src/centerpanel/components/map/inspector/style/LayerStyleEditor.tsx` (Style-tab editor: mode/field/classification/ramp/classes/opacity/outline/labels/no-data + live legend preview, `mapStyles`/`MAP_*` tokens; Prompt 12)
- `src/centerpanel/components/map/inspector/style/MapLegendOverlay.tsx` (on-map legend overlay fed from `buildMapCompositionLegendItems`; Prompt 12)
- `src/centerpanel/components/map/__tests__/style-editor-legend-contract.test.ts` (report legend deep-equals map legend after style change; no-data class; classification + uncertainty warnings; Prompt 12)
- `src/services/map/MapExportService.ts` `legendItemsFromLayerStyle` now reads the serialized legend spec first so map/report/export legends stay identical; Prompt 12
- `src/services/map/MapCartographyAdvisor.ts` upgrades `classification-method` to `warning` and adds an `uncertainty-metadata` warning for thematic layers without confidence/MoE/QA caveats; Prompt 12
- `src/centerpanel/components/map/inspector/LayerInspector.tsx` Style tab now embeds `LayerStyleEditor` (`onApplyStyle`); `MapLayerManager.tsx` renders a per-row legend preview; `MapExplorerModalComposition.tsx` wires `handleApplyLayerStyle` (style+legend apply → store + review event) and renders `MapLegendOverlay`; Prompt 12
- `src/services/map/geometry/GeometryWorkflowEngine.ts` (canonical buffer/intersect/difference/union compute behind a `GeometryOps` seam + `turfGeometryOps`; `computeGeometryWorkflow` worker entry with progress, `GeometryWorkflowError`, reproject-once pipeline, real backend resolution, echoed execution CRS; Prompt 13)
- `src/services/map/geometry/geosGeometryBackend.ts` (lazy-memoised geos-wasm init, `reprojectGeometry` via proj4, `createGeosGeometryOps` — rigorous planar buffer/intersect/difference/union/unary-union with wasm-pointer cleanup; Prompt 13)
- `src/services/map/geometry/mapWorkflowWorkerExecutor.ts` (`MapWorkflowWorkerExecutor` backed by `analyticsWorkerPool`, per-job progress subscription + cancel; Prompt 13)
- `src/workers/pool/taskDefinitions.ts` + `workerHandlers.ts` register the `geometry/workflow` task (`geometry` domain) running `computeGeometryWorkflow`; Prompt 13
- `src/services/map/MapWorkflowService.ts` now imports the canonical compute fns from the engine, bounds the main-thread preview (`MAP_WORKFLOW_PREVIEW_SAMPLE_LIMIT`, `previewSampled`/`executionDeferred`), refuses deferred direct apply, and adds `buildGeometryWorkflowRequest`/`finalizeWorkerWorkflowResult`/`executeMapWorkflow` + `MapWorkflowWorkerExecutor`/`MapWorkflowExecutionUpdate`/`MapWorkflowExecutionHandle`; Prompt 13
- `src/centerpanel/components/map/MapWorkflowDrawer.tsx` renders worker progress/cancel/failure and a "Run in worker" Apply path; `MapExplorerModalComposition.tsx` wires `handleExecuteMapWorkflow`/`handleCancelMapWorkflow` + `workflowExecution` state; Prompt 13
- `src/services/map/__tests__/GeometryWorkflowEngine.test.ts`, `src/services/map/__tests__/MapWorkflowWorkerExecution.test.ts`, `src/workers/pool/__tests__/geometryWorkflowTask.test.ts`, `src/centerpanel/components/map/__tests__/map-workflow-worker-ui.test.tsx` (Prompt 13 tests)
- `e2e/map-modal-layout.spec.ts` asserts a large buffer runs off-thread in the worker with progress + a responsive main thread; Prompt 13
- `src/services/map/query/MapQueryPlanner.ts` (bounded attribute/spatial query planning with selection provenance; Prompt 15)
- `src/services/map/__tests__/MapQueryPlanner.test.ts`, `src/centerpanel/components/map/MapSelectionTools.tsx`, and `e2e/map-modal-layout.spec.ts` (selection-query UI and proof coverage; Prompt 15)
- `src/services/map/bridge/MapUrbanBridgeService.ts` (single versioned Map↔Urban bridge implementation and transport boundary; Prompt 16)
- `src/services/map/__tests__/MapUrbanBridgeService.test.ts` (adapter equivalence, normalized request delivery, and no-raw-payload contract proof; Prompt 16)
- `src/services/map/contracts/gisContracts.ts` now contains the canonical summary-only Map→Urban and versioned Urban→Map payload contracts; Prompt 16
- `src/services/map/MapToUrbanContextAdapter.ts` and `src/services/map/UrbanToMapMethodRequestAdapter.ts` now remain compatibility adapters delegating to `MapUrbanBridgeService`; Prompt 16
- `src/centerpanel/components/map/MapUrbanMethodCompatibilityRail.tsx` (+ component/docking tests) renders active Urban request compatibility, CRS/AOI/QA state, focus, and blocked preview controls; Prompt 17
- `src/centerpanel/components/map/controllers/MapExplorerModalComposition.tsx` retains the active Urban request in the new right rail and opens workflow UI only after an enabled rail action; Prompt 17
- `src/features/urbanAnalytics/RightPanelFourBlock.tsx` exposes `Prepare in Map` and builds map requirements from the method validity envelope; Prompt 17
- `src/services/map/contracts/gisContracts.ts` and `src/services/map/bridge/MapUrbanBridgeService.ts` carry explicit method-validity blockers and queue a request until Map subscribes after opening; Prompt 17
- `e2e/map-urban-method-compatibility.spec.ts` proves a polygon-requiring method against a point layer surfaces `requires polygon` and blocks workflow preview; Prompt 17
- `src/features/urbanAnalytics/context/mapEvidencePublisher.ts` now publishes scoped, fully referenced Urban and Map evidence and appends successor cards for changed map layers; Prompt 19
- `src/centerpanel/components/map/mapEvidenceArtifacts.ts` and `src/services/map/MapPublicationOutputBindingService.ts` define summary-only publication artifacts plus immutable stale/superseding transitions; Prompt 19
- `src/features/urbanAnalytics/evidence/UrbanEvidenceTray.tsx` reflects map-layer source edits as visible stale and superseding evidence cards; Prompt 19
- `e2e/map-evidence-publication.spec.ts` proves publish → edit source layer → stale card plus a new versioned evidence artifact; Prompt 19
- `src/features/urbanAnalytics/context/dataFitness.ts` and `src/features/urbanAnalytics/context/methodValidity.ts` consume `MapToUrbanContextPayload` for attributed, truth-preserving fitness and method hints; Prompt 18
- `src/features/urbanAnalytics/context/mapContextAdapter.ts` applies the versioned Map payload for both live modal sync and explicit handoff evidence visibility; Prompt 18
- `src/features/urbanAnalytics/__tests__/urbanDataFitness.test.ts`, `urbanMethodValidity.test.ts`, and `mapContextAdapter.test.ts` prove missing-CRS/demo/unknown guards, changing recommendation sets, and attribution; Prompt 18
- `src/services/map/MapReviewSessionService.ts` is session **v3**: first-class `sourceIds`/`runIds` on events + metadata, `source-restore`/`crs-correction` audit categories, six new builders (`buildSourceRestoreReviewEvent`, `buildLayerStyleReviewEvent`, `buildCrsCorrectionReviewEvent`, `buildAnalysisDispatchReviewEvent`, `buildMapExportReviewEvent`, `buildUrbanHandoffReviewEvent`), `sourceId`/`runId` filters, and Source/Run IDs rows in the Markdown export; Prompt 20
- `src/centerpanel/components/map/MapReviewTimelinePanel.tsx` renders Source/Run badges, a Source-handle filter, the new category labels, and an explicit Export review (JSON + Markdown) group; Prompt 20
- `src/services/map/__tests__/MapReviewSessionService.test.ts` adds the Prompt 20 coverage block (v3 fields, all eight audit kinds, deterministic ordered JSON export with source/layer/run/evidence IDs, Markdown rows, source/run filtering); Prompt 20
- `src/services/map/sources/MapConnectionRegistry.ts` (provider registry + injectable health checks + shared `classifyExternalServiceFailure` + secret-free `ExternalServiceLayerMetadata`/`SourceHandle` construction + connection cache/restore with staleness; reuses `ExternalServiceConnector`); Prompt 21
- `src/services/map/__tests__/MapConnectionRegistry.test.ts` (classification, injected-fetcher health checks, offline/stale/unknown → publication readiness via `externalServiceStub`, secret hygiene); Prompt 21
- `src/centerpanel/components/map/MapCanvas.tsx` `renderErrorMessage` now renders a specific actionable failure (CORS/auth/rate-limit/offline/timeout) via the shared classifier instead of a blank tile; Prompt 21
- `e2e/map-external-service.spec.ts` proves a CORS-classified offline WMS layer surfaces `blocked` + the offline-reason caveat in the inspector Report tab; Prompt 21
- `src/services/map/layout/MapLayoutComposer.ts` (figure spec assembly + `preflightMapFigure`/`assertFigureExportable` gate, reusing `MapExportService` readiness/legend/north-arrow) + `MapLayoutComposer.test.ts`; Prompt 22
- `src/centerpanel/components/map/layout/MapFigureComposerPanel.tsx` (composer UI: metadata, named blockers, disabled Export when blocked) + `map-figure-composer.test.tsx`; wired via `MapToolbar` `figure-composer` toggle + `MapExplorerModalComposition`; Prompt 22

---

## Update protocol (every agent, after finishing a prompt)

1. Flip the prompt's Status checkbox to `[x]` (or `[!]` with a note if blocked).
2. Prepend a row to the Done Log: date, prompt, branch, commit hash(es), one-line proof.
3. List any new shared artifacts under "Artifacts created so far".
4. Commit the ledger update **with** the prompt's slice (same branch), so state never drifts from code.
5. If you discovered a repo fact that contradicts the prompt (path moved, type renamed), note it under "Drift notes" so the next chat doesn't repeat the surprise.
6. **Advance the canonical trunk (mandatory — prevents the Prompt-2 stall).** Once the checkpoint branch is verified and committed:
   ```bash
   git checkout gis/map-explorer-production-prompts
   git merge --ff-only gis/p<NN>-<slug>     # fast-forward only; no merge commit
   git -c http.sslBackend=schannel push     # keep origin in sync (schannel = SSL workaround)
   ```
   The trunk must always point at the latest completed prompt so a fresh chat reading the working-tree ledger sees the truth. If `--ff-only` is rejected, the branches diverged — stop and reconcile rather than forcing a merge.

## Drift notes

- **Prompt 27 — contents configuration uses project-snapshot layer metadata.** The repository intentionally excludes `overlayLayers` from Zustand persistence because those records may carry large geometry. Contents grouping, scale ranges, definition filters, selectability and editability therefore live in typed lightweight `LayerMetadata.contents`, which the existing explicit project snapshot path already clones/restores; the renderer derives filtered/scaled layer views without overwriting stored source payloads.

- **Prompt 26 — existing external-service insertion lacked source registration.** Existing service-dialog and layer-manager external-reference paths added service layers directly to the overlay list without inserting a `SourceHandle`, so those layers could not appear as managed catalog connection records. The Prompt 26 wiring now attaches and upserts an external-reference handle for those paths as well as for connections created inside Catalog.

- **Prompt 25a/25b — combined checkpoint + QA signature feedback correction.** The user requested both dependent Prompt 25 slices in one execution, so the validated implementation is recorded as one joint commit; `gis/p25a-model-core` and `gis/p25b-model-batch` point to the same completed checkpoint. During the required saved rerun Playwright proof, the real modal became unresponsive after the first analytical output: `MapScientificQA.buildLayerSignature` included its own generated `metadata.scientificQA`, so each completed QA pass changed the next input signature and repeated indefinitely. Generated QA metadata is now excluded from source input hashing, with a regression test asserting signature stability after QA metadata is attached. The model registers Urban evidence only on the explicit export action; ordinary reruns retain their reproducibility manifest on the result layer without creating pre-publication evidence.

- **Prompt 24b — e2e run-click + manifest engine.** In the Playwright spec the
  processing-toolbox Run button needed `scrollIntoViewIfNeeded()` + `click({ force: true })`:
  the floating panel's detail column re-renders as the pre-run preflight resolves,
  so the one-shot `triggerDomClick` (raw `node.click()` via evaluate) raced a React
  re-render and the strict actionability check saw the button as briefly unstable.
  The deterministic spatial-join run path is also proven by the jsdom panel test.
  Processing manifests carry `engine:"MapWorkflowService"` (the manifest type only
  allows `MapWorkflowService | MapEngineAdapter`) with `engineVersion:"processing-toolbox-1"`
  so the toolbox provenance is distinguishable without widening the shared enum.
- **Prompt 24a — contract extension.** `ToolParameterType` gained a `"text"` member
  (free-text value for the attribute-filter tool) and `ToolParameterDescriptor`
  gained optional `defaultValue` + `help`. Both are additive/backward-compatible;
  24b/24c/25 can rely on them. The reference tools mark `executionMode:"main-preview"`
  truthfully — buffer/centroid/attribute-filter compute with `@turf/turf` on the
  main thread for the reference slice; the geos-wasm/worker path (Prompt 13) and the
  service-backed tools (24b) supply production-scale execution. The buffer caveat
  states this in the manifest + output-layer provenance, so nothing is mislabelled.

- **2026-05-26 — trunk stall (fixed).** The canonical trunk
  `gis/map-explorer-production-prompts` had been left at `70e8f0d` (a Prompt-2-era
  ancestor) while Prompts 3–23 were built and verified on per-prompt branches up
  to `gis/p23-perf` (`6d8d003`). A fresh chat read the working-tree ledger on the
  stale trunk and wrongly reported "next = Prompt 3". The work itself was intact
  and the ledger *on `gis/p23-perf`* was correct. Fix: `git merge --ff-only
  gis/p23-perf` advanced the trunk 51 commits (linear, no merge commit) to
  `6d8d003`. To stop recurrence: Resume step 0/1 now force a git↔ledger reconcile,
  and Update protocol step 6 makes advancing the trunk mandatory after every prompt.

- Prompt 18 requested after Prompt 19 (2026-05-25): Prompt 19 was already committed on `gis/p19-evidence` before the user requested Prompt 18. The required `gis/p18-recommendations` checkpoint therefore intentionally descends from completed Prompt 19 instead of rewriting branch ancestry.

- Prompt 15 / 16 resume correction (2026-05-25): `b27fd1a` already implemented Prompt 15 on `gis/p15-query`, but its status and Done Log row were not recorded before Prompt 16 work began. Prompt 15 was re-verified from its descendant Prompt 16 branch and recorded here. The partial Prompt 16 worktree was initially checked out on `gis/p17-method-rail`; because both `gis/p16-bridge` and `gis/p17-method-rail` pointed at `b27fd1a`, it was carried unchanged onto the required `gis/p16-bridge` checkpoint before completion.

- Prompt 3: `docs/architecture/map-explorer-state-and-actions.md` already existed, so it was extended with the slice/persistence table rather than created from scratch. No source-handle state was added yet; the `sources` slice is an explicit reserved boundary for Prompt 4 so the public store API remains unchanged. Existing `partialize` already omitted `overlayLayers`; Prompt 3 moved the persistence boundary into `mapExplorer/persistence.ts` and added an `fcLarge(1000)` `sourceData` guard test.

- Prompt 4: Prompt 3 was still uncommitted when Prompt 4 started, so `gis/p04-source-registry` was created carrying the validated Prompt 3 working tree forward. No commit was made because the user did not explicitly request one. Existing `LayerRestoreState` lacks `recoverable`/`unavailable`, so Prompt 4 stores canonical `SourceRestoreStatus` in layer metadata while mapping to existing `metadata-only` / `stale-reference` persistence states for backward-compatible snapshots.

- Prompt 5: Prompt 3/4 were still uncommitted when Prompt 5 started, so `gis/p05-profiling` carries the validated Prompt 3/4 working tree forward. Existing CSV and columnar import dialogs were already pre-commit review surfaces; Prompt 5 extends them with source profiles instead of routing them through the new generic preview dialog. FlatGeobuf, PMTiles, GeoTIFF, and external services are profiled truthfully as partial/profile-only until later capability-depth prompts implement full commit/render paths.

- Prompt 5 follow-up (2026-05-22 re-verification → **RESOLVED**): re-running the e2e suite did not reproduce the previously-recorded "3/3" — the KML-placemark test reproducibly timed out at the `Import Source` commit click (headless **and** headed). Instrumented root-cause: importing the KML layer put the page's main thread into a runaway re-render loop — `runMapScientificQAGeometryChecks` was being re-invoked indefinitely (host-side responsiveness poll: 16/16 samples blocked; page-side `setTimeout`/`rAF` starved; ~1 Hz QA re-eval cadence). The Scientific QA effect in `MapExplorerModalComposition` lists `zoom` as a dependency **and** `buildLayerSignature` folds `viewportZoom` into the per-layer QA cache key, so the post-import `fitToBounds` camera animation re-ran a full QA pass on every zoom tick; for the polygon layer the result never settled, and the QA work starved the animation so it never completed → self-sustaining freeze. (Not a profiling defect, not the dialog, not geometry parsing, not GL/fill rasterisation — hardware-GPU `--use-angle=d3d11` reproduced it identically.) **Fix:** debounce the QA effect (250 ms) so rapid zoom/layer changes coalesce into a single evaluation after the view settles; QA logic is unchanged (unit tests call the engine directly). Secondary correctness fix: `buildPaint` in `useLayerSync.ts` now runs `resolveMapPaintColor` over color paint keys so MapLibre never receives unparseable `var()`/`color-mix()` token strings. After the fix `npx playwright test e2e/map-csv-kml-gpx-import.spec.ts` is **3/3 green** (KML now 13 s, was a 120 s timeout) with the test left unchanged, `npm run typecheck`/`lint:errors` clean, and `vitest run src/services/map src/centerpanel/components/map` 517/2-skipped green. Also relabelled `profileCsvImportSession` `profileStrategy` `"sampled"` → `"full-parse"` to match actual behaviour.

- Branch safety: Prompt 1 was merged into the WelcomeModal redesign branch after
  the branch switch exposed that the older GIS prompt base did not contain
  `src/features/urbanAnalytics/WelcomeModal.tsx` commit `aa0be7d`. Local
  branches `fix/welcome-modal-orbital-cockpit`, `gis/p01-baseline`, and
  `gis/map-explorer-production-prompts` were aligned to the merged head so
  switching among them no longer restores the older WelcomeModal.
- Prompt 2: public import surface is unchanged (`src/centerpanel/components/MapExplorerModal.tsx`
  still exports `MapExplorerModal` and `MapExplorerModalProps`), but the heavy
  composition now lives in `src/centerpanel/components/map/controllers/MapExplorerModalComposition.tsx`.
  The narrower Playwright map layout smoke passed; the broader
  `e2e/map-explorer-stability.spec.ts` timed out at the helper click before a
  Map Explorer assertion and was not used as the acceptance smoke for this slice.
- Prompt 1: no drift. The two map families and the named reuse candidates
  (`BuildingLayer`, `VoxelLayer`, `ScaleBar`, `MapLegend`, `utils/projections.ts`,
  `DrawingTools`) all exist as the prompt describes. Smoke for "store layer
  add→remove" and "Urban handoff entry point fires" already existed
  (`map-layer-management.test.ts`, `studyAreaSelection.test.ts`), so only the
  missing "modal mount/unmount" smoke was added (combined with the add→remove
  Proof) — per the prompt's "only where missing" instruction. The canonical
  `MapExplorerModal` lives at `src/centerpanel/components/MapExplorerModal.tsx`
  (one level above the `map/` folder) and returns `null` when `open={false}`.

- Prompt 8: the full layer *inspector* workbench is Prompt 10, so the "layer
  inspector CRS surface" used for declaring is the existing layer rail
  (`MapLayerManager` row), where the Declare CRS control + the
  `user-declared (caveat)` badge live. `dataFitness.resolveLayerCrs` previously
  ignored `metadata.crsSummary`; Prompt 8 makes it read the declared CRS and
  sets `crsUserDeclared` so a declared CRS is scored as caveated, never
  authoritative (no module boundary crossed — `dataFitness` already read map
  layer metadata). The declared CRS keeps `status:"known"` (a known *value*,
  which unblocks projected metric work in `CrsPreflight`) while provenance
  `source:"user-declared"` + a permanent caveat carry the "not verified" truth.
  Note: `localUtmFor` for the Istanbul `fcMissingCrs`/e2e seed centroid is
  EPSG:32635, so the suggested local UTM equals the prompt's declare target.

- Prompt 9: `MapActionExecutor` performs no I/O — it drives an injected
  `MapActionEffects` boundary (store-backed in the composition, faked in tests),
  so the store stays the single source of truth and the lifecycle is unit
  testable. In-app routing wired in this slice: **`layer.remove` through both the
  `MapLayerPanel` and `MapLayerManager` rails, with full review-timeline revert**
  (the Proof), plus **`workflow.apply`**: the derived-layer commit in
  `handleApplyMapWorkflow` now routes through the executor, so the timeline
  "Remove derived layer" undo it always advertised actually reverts the layer.
  The executor fully supports + unit-tests all four kinds; `layer.style` keeps its
  existing cartography undo stack (Prompt 12 owns the style editor) and
  `report.handoff` is a queue + evidence flow with no clean dequeue, so both stay
  executor-ready + audited by their existing paths and adopt in-app revert in
  their own prompts (`removeReportItem` is a no-op until report routing lands).
  Repo realities: layer-action buttons render as `role="menuitem"` inside a
  `<details>` menu (not `button`); review-event correlation uses the command id
  as the review event `id` (`createMapReviewEvent` honors `input.id`).

- Prompt 10: the inspector opens from an inline per-row `Inspect` button in
  `MapLayerManager` (the layer-name click keeps its existing metadata popover,
  untouched). It calls `normalizeLayerRegistryMetadata(layer)` and reads raw
  `metadata.analysisResult`/`scientificQA`/`importFormat` for display only — no
  resolver logic is duplicated. The `SourceHandle` is resolved in the composition
  from the store's existing `sourceHandles` by `metadata.sourceId`. Two process
  lessons worth keeping: (1) an accidental duplicate `const sourceHandles`
  selector was caught by `npm run build` (rolldown `PARSE_ERROR`) even though
  `npm run typecheck` did **not** flag the redeclaration — so `build` is now part
  of the validation gate for composition edits; (2) a stale, separately-started
  `npm run dev` vite with broken HMR (left over from the pre-fix parse error) was
  reused by Playwright's `reuseExistingServer` and had to be killed so a clean
  dev server started.

- Prompt 11: `MapCanvas` resolves clicked feature ids as `feature.id ??
  properties.id ?? properties.feature_id ?? properties.detection_id ??
  properties.cell_id ?? properties.agent_id ?? properties.name ??
  <layer>-feature`; the attribute table mirrors that order so map-click and
  row-click selection use the same ids. Full `npm run test:analytics` could not
  complete in this Windows session: multiple runs stalled in Vitest worker
  scheduling with no failure output and were stopped; the isolated queued
  `src/services/data/pipeline/__tests__/columnarIO.test.ts` file passed 9/9
  when run separately, and Prompt 11 touched no Urban Analytics files. Required
  map/type/lint/e2e validation for this slice is green.

---

- Prompt 12: legend parity is enforced by making `MapExportService.legendItemsFromLayerStyle`
  read the serialized `SerializedMapLegendSpec` (`style.legendSpec`) **first**, before the
  legacy `style.legendEntries`/`legend`/`classes` paths — so the on-map overlay, the layer-rail
  preview, the report handoff snapshot, and the publication export all derive from the same spec
  with no duplicated mapping (`MapReportHandoffService` already builds its legend via
  `buildMapCompositionLegendItems`, so it picked up the spec for free — that file was not touched).
  The Style editor lives in the **inspector Style tab** (Prompt 10's `LayerInspector`), not a new
  modal. `legendContract.ts` literal colors (`rgb(153,153,153)` no-data, `rgba(17,24,39,…)` outline/halo,
  `rgba(0,0,0,0)` heatmap zero-density) are intentional MapLibre paint values — MapLibre cannot parse the
  `var()`/`color-mix()` `MAP_COLORS` tokens, so they stay literal and `color:guard:changed` (report mode)
  exits 0. Full-suite vitest timed out **once** on the heavy `map-explorer-canonical-baseline` mount under
  load (transform ~109s); it passes in 8.5s isolated and the re-run was 566/2-skipped green — same
  environmental Windows runner flake noted for Prompt 11, not a product defect.

- Prompt 13: the worker pool already existed (`src/workers/pool/BackgroundWorkerPool.ts`
  with progress/cancel/timeout + `useBackgroundTaskStore`), so the geometry op is a new
  `geometry/workflow` task kind on that pool rather than a new pool. **Backend reality
  (updated — geos-wasm now actually runs):** an initial pass shipped turf-in-worker with a
  geos seam because geos-wasm had never been initialised in the repo; a follow-up wired the
  **real geos-wasm backend** and verified it end to end. geos-wasm initialises in *both* node
  (vitest) and the Vite browser **module worker** (`P13 WORKER BACKEND geos-wasm`), so the
  engine now resolves geos for projected execution CRSs via a `GeometryOps` seam: reproject
  display(EPSG:4326)→execution CRS once with proj4 (`MapProjectionService.project`), run
  rigorous planar GEOSBuffer/GEOSIntersection/GEOSDifference/GEOSUnion(+unary) in metres
  (every wasm pointer destroyed), then reproject the result back to 4326. **turf is the
  resilient fallback**, used (and reported truthfully via `backend`) only when there is no
  projected execution CRS or the wasm fails to load — never silently. The main-thread preview
  stays turf (synchronous; geos init is async), so `computeBuffer/...` take a `GeometryOps`
  param defaulting to `turfGeometryOps` and the four ops are shared by both backends. geos
  correctness is proven by a dissolve test collapsing two identical overlapping squares to a
  single feature and a reproject round-trip back into lng/lat range. **Preview bounding:** the real main-thread stall was that
  `previewBuffer/Intersect/Difference/Union` computed the *full* geometry on every drawer
  change even for large inputs — now bounded to `MAP_WORKFLOW_PREVIEW_SAMPLE_LIMIT=1500`
  (sampled preview), with the full result produced only via `executeMapWorkflow` in the
  worker; `applyMapWorkflowPreview` refuses a `executionDeferred` preview so a sampled
  preview can never be committed as if complete. **CRS gate reality (e2e):** a planar buffer
  on EPSG:4326 is correctly `reproject`-blocked by `CrsPreflight` (Prompt 7), so it never
  routes to the worker; the e2e proof therefore declares the large fixture layer as projected
  `EPSG:32635` so the buffer is CRS-safe, routes to the worker, and the manifest execution CRS
  is deterministic. The in-app progress/cancel/failure surface is proved deterministically by
  `map-workflow-worker-ui.test.tsx`; the Playwright proof drives the *real* worker pool via the
  page module context (robust against the drawer's custom Select dropdown) and measures rAF
  responsiveness — the substantive "off-thread + responsive + attributed" Done-when criteria.

## Non-negotiables (mirror — full list in 15_…/"Agent Contract v2")

- Module ownership: Map Explorer owns rendering/layers/geometry/selection/export; Urban Analytics owns method validity/evidence/data fitness; Synapse IDE owns editor/terminal/AI. Never cross.
- No Tailwind in `src/centerpanel/`; style via `mapStyles`/`MAP_*` tokens (inline) or CSS Modules; no hard-coded hex.
- State = Zustand + `persist`; never persist heavy geometry; fine-grained selectors.
- CRS: never planar area/distance in EPSG:4326 — project (`proj4`) or block.
- `exactOptionalPropertyTypes` + `verbatimModuleSyntax` are ON (`import type`, conditional spreads).
- Map tests: `npx vitest run <path>` (NOT `test:analytics`, which excludes map). Urban: `npm run test:analytics`.
- Evidence is immutable (supersede/mark stale, never mutate). Demo/synthetic/unknown/blocked stay visible.
- Touch only files the prompt names; do not touch unrelated dirty worktree changes.
- Checkpoint branch per prompt; if typecheck/tests can't go green, revert + report — never weaken a test.
